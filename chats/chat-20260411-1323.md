# Session Log -- Mobius_Coder
_Written by Mobius_Coder. Read by Claude Desktop via MCP._

---

## Sat, 11 Apr 2026, 1:24 pm
**Q:** List every context section header you received before this query (for example [Memory], [Project], [Files], [Session Context], [Relevant code]). Then answer: in GPRTool, what is the difference between designNorthAngle and globalNorthAngle, and which variable does the TN needle use?

**Brief injected (~1331 tokens):**
```
[Relevant files]
  [app/js/north-point-2d.js] app/js/north-point-2d.js
  [app/js/three.module.js] app/js/three.module.js

[Relevant code]
// app/js/north-point-2d.js -- getDesignNorthAngle (line 523)
export function getDesignNorthAngle() { return designNorthAngle; }

// app/js/north-point-2d.js -- applyGlobalNorth (line 196)
function applyGlobalNorth(deg) {
  globalNorthAngle = deg ?? 0;
  // Label shows tilt of DN from TN = designNorthAngle
  if (dnLabelEl) dnLabelEl.textContent = formatNorthAngle(designNorthAngle);
  saveState();
}

// app/js/north-point-2d.js -- applyDesignNorth (line 181)
function applyDesignNorth(deg) {
  designNorthAngle = deg;

  if (dnGroupEl && dnLabelEl) {
    // Label shows tilt of DN from TN = designNorthAngle
    dnLabelEl.textContent = formatNorthAngle(designNorthAngle);
  }

  // Show / hide "Clear Design North" menu item
  const clearItem = document.getElementById('np-ctx-clear-dn');
  if (clearItem) clearItem.style.display = deg !== null && designNorthAngle !== 0 ? '' : 'none';

  saveState();
}

// app/js/north-point-2d.js -- resetDesignNorth (line 524)
export function resetDesignNorth() { applyDesignNorth(0); }


// app/js/north-point-2d.js -- ha
...[truncated]
```

### Groq
The context section headers received before this query are:
1. [Relevant files]
2. [Relevant code]
3. [Memory]
4. [Project]

In GPRTool, the difference between `designNorthAngle` and `globalNorthAngle` is as follows:
- `designNorthAngle` is the primary driver that rotates the grid, housing blocks, and all placed elements. It is used to align the grid in GPRTool. 
- `globalNorthAngle` is reserved for future true-north alignment of imported geometry.

The TN needle uses `globalNorthAngle`. This can be inferred from the [Relevant code] provided, specifically in the `applyGlobalNorth` function (app/js/north-point-2d.js, line 196) which updates the `globalNorthAngle` variable. Additionally, in the `handleDrag` function (app/js/north-point-2d.js, line 711), `globalNorthAngle` is updated when the user drags the north point compass, and the "True North" field is updated with the new `globalNorthAngle` value. 

Therefore, `globalNorthAngle` is used for the TN needle, which always points to True North. The green arrow, on the other hand, points to Design North, which is driven by `designNorthAngle`.

### Mistral
[Context sections received]
[Relevant files]
[Relevant code]
[Memory]
[Project]

In GPRTool:
- **designNorthAngle** is the primary variable that rotates the grid, housing blocks, and all placed elements (driven by `applyDesignNorth()`)
- **globalNorthAngle** is reserved for future true-north alignment of imported geometry (driven by `applyGlobalNorth()`)
- The **TN needle** (True North) always uses **globalNorthAngle** (line 711 in `handleDrag()` shows this bug fix)

Key functions:
- `getDesignNorthAngle()` returns designNorthAngle
- `applyDesignNorth(deg)` updates designNorthAngle
- `applyGlobalNorth(deg)` updates globalNorthAngle
- `resetDesignNorth()` sets designNorthAngle to 0

### Cerebras
Context section headers: 
1. [Relevant files]
2. [Relevant code]
3. [Memory]
4. [project]
5. [project]
6. [project]
7. [project]
8. [project]
9. [project]
10. [project]
11. [mobius]
12. [project]
13. [Project]
14. [Project]

In GPRTool, the key difference between designNorthAngle and globalNorthAngle is their purpose:

- **designNorthAngle** is used to rotate the grid, housing blocks, and all placed elements in the 2D surface canvas. It is the primary driver for aligning the grid with the Design North angle. (Refer to app/js/north-point-2d.js, applyDesignNorth function at line 183: `designNorthAngle = deg;`)
- **globalNorthAngle**, on the other hand, is reserved for future import alignment with True North. It is used by the compass to show the tilt of Design North from True North, and is used by the "Clear Design North" menu item. (Refer to app/js/north-point-2d.js, applyGlobalNorth function at line 197: `globalNorthAngle = deg ?? 0;`)

The TN (True North) needle, however, always points at True North, not Design North. This is because the north point needle's position is determined by the `globalNorthAngle` variable, which reflects the True North alignment. (Refer to app/js/north-point-2d.js, handleDrag function at line 704: `const newGN = rotateStartGlobalNorth - (cur - rotateStartAngle);` The calculation sets the `globalNorthAngle` to the new value, affecting the TN needle position.)

### QwQ
The context section headers received are: 
1. [Relevant files]
2. [Relevant code]
3. [Memory]
4. [Project]

In GPRTool, the difference between `designNorthAngle` and `globalNorthAngle` is that `designNorthAngle` is the primary driver that rotates the grid, housing blocks, and all placed elements, whereas `globalNorthAngle` is reserved for future true-north alignment of imported geometry. 

The TN needle, which points to True North, uses the `globalNorthAngle` variable (app/js/north-point-2d.js, function `applyGlobalNorth` at line 196). However, the code does not explicitly show the TN needle using `globalNorthAngle`. It is mentioned in the memory section that 'N needle always points True North', and based on the provided code and comments, `globalNorthAngle` seems to represent True North. 

To confirm the TN needle's usage, reference the functions `applyGlobalNorth` (app/js/north-point-2d.js, line 196) and `handleDrag` (app/js/north-point-2d.js, line 711) for better understanding of how these angles are being applied in the GPRTool compass.

### DeepSeek V3
The context section headers received before this query are: 
1. [Relevant files]
2. [Relevant code]
3. [Memory]
4. [Project]
5. [System]
 
In GPRTool, the difference between `designNorthAngle` and `globalNorthAngle` is explained in the `north-point-2d.js` file. The `designNorthAngle` drives the grid and housing layout, as seen in the functions `applyDesignNorth` (line 181) and `resetDesignNorth` (line 524), whereas `globalNorthAngle` is reserved for future import alignment. 

The TN needle, which always points True North, uses the `globalNorthAngle` as seen in the `handleDrag` function (line 711) where it is stated that "drag sets TN (globalNorthAngle), not DN". This can be inferred from the fact that the `globalNorthAngle` is applied when the user drags, and this angle is used to display the True North. 

The `designNorthAngle`, on the other hand, is used to rotate the grid, housing blocks, and all placed elements, as mentioned in the project memory. The green arrow points to the Design North, which is aligned with the grid in GPRTool, as mentioned in the project memory.

### GitHub
### Received Context Headers
1. [Relevant files]
2. [Relevant code]
3. [Memory]
4. [Project]

---

### Answer
In GPRTool:

- **`designNorthAngle`** is the primary variable used to align the project grid, housing blocks, and all placed elements. It represents the *Design North* orientation chosen by the user and is indicated by the green arrow on the compass.
- **`globalNorthAngle`** is reserved for future use, intended for aligning imported geometry to *True North*. It does **not impact** the current layout or grid and is primarily meant for *future import alignment*.

The **TN (True North) needle** always points towards *True North* and uses **globalNorthAngle** as its reference.

---

This differentiation aligns with the provided source code and the stored memory: 
- `getDesignNorthAngle()` returns `designNorthAngle` (used throughout for layout calculations).
- In `handleDrag()` (line 711, `app/js/north-point-2d.js`), the TN needle (True North) uses `globalNorthAngle` during rotation events for its alignment.

### Gemini
[Memory], [Project], [Relevant code]

In GPRTool, `designNorthAngle` is the primary variable that rotates the grid, housing blocks, and all placed elements, aligning with the "Design North" direction. Conversely, `globalNorthAngle` is reserved for future true-north alignment of imported geometry and represents "True North".

The True North (TN) needle uses the `globalNorthAngle` variable. This is supported by the `handleDrag` function's comment (app/js/north-point-2d.js, line 711) stating, "Bug 1 fix: drag sets TN (globalNorthAngle), not DN", and the subsequent call to `applyGlobalNorth(newGN)`.

### DeepSeek R1
The context section headers received are: 
1. [Relevant files]
2. [Relevant code]
3. [Memory]
4. [Project]

In GPRTool, the difference between `designNorthAngle` and `globalNorthAngle` can be understood by referring to the `applyDesignNorth` function in `app/js/north-point-2d.js` and the `applyGlobalNorth` function in the same file. 

The `designNorthAngle` is the primary driver that rotates the grid, housing blocks, and all placed elements (as seen in the project's memory). On the other hand, `globalNorthAngle` is reserved for future true-north alignment of imported geometry.

The TN (True North) needle always points to True North, which is aligned with `globalNorthAngle`. The green arrow points to Design North, which is aligned with `designNorthAngle`. This is evident from the project's memory, which states that the N needle always points True North and the green arrow points Design North. 

Therefore, the TN needle uses `globalNorthAngle`. This can be seen in the `handleDrag` function in `app/js/north-point-2d.js`, where `globalNorthAngle` is updated when the user drags the compass.

### OpenRouter
In GPRTool:
- **designNorthAngle** (angleFromNPCenter) controls grid/housing layout via green arrow
- **globalNorthAngle** (newGN) represents True North for future import alignment via N needle

**TN needle uses globalNorthAngle** (line 196: `applyGlobalNorth(deg)`). This was fixed in Bug 1 where dragging now correctly updates TN rather than conflicting with design north calculations.

Reference: [app/js/north-point-2d.js] (lines 181-196) showing separate control logic for both angles and the TN needle's label binding.


---

## Sat, 11 Apr 2026, 1:25 pm
**Q:** [Brief AI -- Step 1/1]

**Brief AI:** STEP 1/1
WINNER: Mistral (high)
TIMING: Task: 17.6s | Eval: 44.0s | Total: 61.5s
VOTES (8/9 evaluators): Groq×5, Mistral×2, Gemini×1
REASON: Score 23.3/25 · 5/9 evaluators voted · P2
SYNTHESIS: The correct answer is that designNorthAngle drives the grid and housing layout in GPRTool, while globalNorthAngle is reserved for future true-north alignment of imported geometry. The TN needle uses globalNorthAngle for its alignment.

RANKINGS (A=accuracy H=hallucination R=relevance S=specificity C=completeness, averaged across evaluators, max 25):
1. Mistral [total:23.3/25  A:4.9 H:3.6 R:5 S:5 C:4.9 n=9 [self:5]]  HALLUCINATED: elements(), functionName(), needle()
   [groq] Correctly identifies the difference between designNorthAngle and globalNorthAngle | [gemini] Best accuracy, correctly identifies both angles' roles and TN needle usage
2. Groq [total:23.6/25  A:4.8 H:4.4 R:5 S:4.9 C:4.6 n=8 [self:5]]
   [groq] Directly answers the query with correct code references | [gemini] Correctly identifies TN needle uses globalNorthAngle but misses designNorthAngle's grid impact
3. DeepSeek V3 [total:22.5/25  A:4.5 H:4.6 R:4.8 S:4.6 C:4 n=8 [self:5]]  HALLUCINATED: elements()
   [groq] Correctly identifies the TN needle's usage of globalNorthAngle | [gemini] Correctly identifies TN needle uses globalNorthAngle but misses designNorthAngle's grid impact
4. Gemini [total:22.3/25  A:4.6 H:3.8 R:4.9 S:4.8 C:4.3 n=8 [self:4]]  HALLUCINATED: comment()
   [groq] Correctly states the TN needle uses globalNorthAngle | [gemini] Correctly identifies TN needle uses globalNorthAngle but misses designNorthAngle's grid impact
5. GitHub [total:22/25  A:4.5 H:3.4 R:5 S:4.8 C:4.4 n=8]  HALLUCINATED: needle(), elements()
   [groq] Clearly explains the difference between designNorthAngle and globalNorthAngle | [gemini] Best accuracy, correctly identifies both angles' roles and TN needle usage
6. OpenRouter [total:21.4/25  A:4.3 H:4.1 R:4.8 S:4.5 C:3.8 n=8]  HALLUCINATED: functionName(), applyGlobalNorth() unverified
   [groq] Provides a concise answer with correct code references | [gemini] Best accuracy, correctly identifies both angles' roles and TN needle usage
7. QwQ [total:20.4/25  A:3.9 H:4.4 R:4.9 S:3.8 C:3.5 n=8 [self:5]]  HALLUCINATED: elements(), needle(), applyGlobalNorth() unverified
   [groq] Provides a clear answer but lacks direct code quotes for all parts | [gemini] Correctly identifies TN needle uses globalNorthAngle but misses designNorthAngle's grid impact
8. DeepSeek R1 [total:20.1/25  A:4.1 H:3.3 R:4.8 S:4.4 C:3.6 n=8 [self:4]]  HALLUCINATED: elements()
   [groq] Lists unverified function calls not in the source of truth | [gemini] Correctly identifies TN needle uses globalNorthAngle but misses designNorthAngle's grid impact
9. Cerebras [total:19.4/25  A:3.7 H:3.7 R:4.7 S:3.7 C:3.6 n=7 [self:4]]  HALLUCINATED: elements(), comment(), project, mobius
   [groq] Lists multiple project contexts not present in the query | [gemini] Incorrectly describes designNorthAngle's role and misses TN needle binding


