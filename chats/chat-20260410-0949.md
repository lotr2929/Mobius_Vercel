# Session Log -- Mobius_Coder
_Written by Mobius_Coder. Read by Claude Desktop via MCP._

---

## Fri, 10 Apr 2026, 9:49 am
**Q:** In js/brief.js, the autoEvaluate function sends this exact prompt to Gemini Lite to score 5 AI responses: 'Score these AI responses to a coding query. Reply ONLY with valid JSON. Query: [query]. Responses: [model]: [content]. JSON: {winner, reason, synthesis, confidence}'. This prompt has no scoring rules so Gemini Lite picks the most confident-sounding answer regardless of correctness. Write a revised evalPrompt string (JavaScript string literal, no template literals) that adds explicit scoring rules: correctness over conciseness, penalise invented details not confirmed by other responses, reward honesty when code is not available, penalise generic non-specific advice. Keep the same JSON output format.

**Brief injected (~565 tokens):**
```
[Relevant files]
  [api/auth/[service].js] api/auth/[service].js

[Memory]
[mobius] Mobius_Coder scores.js tracks localStorage model win/loss scores -- used by router to weight model selection
[project] Mobius_Coder Code: command family: generate, fix, explain, review, file -- handled in js/code.js
[mobius] Mobius_Coder All Mode prepends brief + slim + lastReadFile content to every model query via buildContext() in all.js
[mobius] Mobius_Coder api/_ai.js contains all model functions and fallback chains -- cloud chain: gemini-lite -> groq -> mistral -> github
[mobius] Mobius_Coder js/router.js implements conversational router -- assigns each query to best model by detected task category (code/debug/explain/plan/review)
[project] Mobius_Vercel has complexity-based AI routing via scoreComplexity() and self-test framework in self_test.js
[tools] Gemini Lite quota errors should fall back to Groq cascade -- wrap Gemini calls in try-catch and check error.message.includes("quota")
[mobius] Mobius_Coder memory system auto-extracts facts after every AI response via autoExtractMemory() -- writes Q+A pair to memory_general for later distillation
[project] Mobius_Coder Debug pipeline: 5 steps e
...[truncated]
```

### Groq Llama 3.3 70B
```javascript
const evalPrompt = "Score these AI responses to a coding query, prioritising correctness over conciseness. In your evaluation, penalise responses that introduce invented details not confirmed by other responses. Reward honesty when the correct code is not available. Conversely, penalise generic, non-specific advice. Reply ONLY with valid JSON. Your response should be in the following format: {winner, reason, synthesis, confidence}. Query: [query]. Responses: [model]: [content].";
```

### Groq Llama 3.3 70B (fallback from Gemini)
```javascript
const evalPrompt = "Score these AI responses to a coding query, prioritising correctness over conciseness. Penalise responses that invent unconfirmed details and reward honest admissions when code is not available. Penalise generic, non-specific advice. Reply ONLY with valid JSON. Query: [query]. Responses: [model]: [content]. JSON: {winner, reason, synthesis, confidence}";
```

### Cerebras Llama 3.1 8B
```javascript
let evalPrompt = "Score these AI responses to a coding query.\n" +
                 "Reply ONLY with valid JSON.\n" +
                 "Query: [query].\n" +
                 "Responses: [model]: [content].\n" +
                 "JSON:\n" +
                 "  winner: the response with the most accurate code,\n" +
                 "  reason: 'accuracy',\n" +
                 "  synthesis: concatenate all code snippets,\n" +
                 "  confidence: (weight for most specific response, -weight for generic advice, -weight for invented details) \n" +
                 "  where weight=0.2 for accuracy and -0.1 for each of invented details and generic advice.";
```

I've preserved the original prompt's style and structure, incorporating the new scoring rules. Note that weight values are arbitrary and can be adjusted according to specific requirements.

The revised evalPrompt string includes the following features:

- Prioritises the response with the most accurate code.
- Rewards the response that is most specific and accurate.
- Penalises responses with generic, non-specific advice (weight = -0.1).
- Penalises responses with invented details not confirmed by other responses (weight = -0.1).
- Rewards honesty when code is not available.
- Preserves the JSON output format.

### Codestral
Here's the revised `evalPrompt` string with explicit scoring rules:

```javascript
const evalPrompt = 'Score these AI responses to a coding query. Reply ONLY with valid JSON. Query: [query]. Responses: [model]: [content]. JSON: {winner, reason, synthesis, confidence}. Scoring rules: 1. Correctness over conciseness - prioritise accurate solutions. 2. Penalise invented details not confirmed by other responses. 3. Reward honesty when code is not available. 4. Penalise generic non-specific advice. 5. Confidence should reflect actual correctness.';
```

This maintains the same JSON output format while adding clear evaluation criteria.

### OpenRouter: gemma-3n-e4b-it
```javascript
'Score these AI responses to a coding query. Reply ONLY with valid JSON. Query: [query]. Responses: [model]: [content]. JSON: {winner, reason, synthesis, confidence}. Scoring rules: Prioritise correctness over conciseness. Penalise invented details not corroborated by other responses. Reward honesty if code is unavailable. Penalise generic or non-specific advice. Winner should be the model with the highest confidence score *and* a reason stating correctness and lack of invention.'
```

---

## Fri, 10 Apr 2026, 9:49 am
**Q:** [Brief eval step 2]

**Brief AI:** [Brief AI eval] Winner: Cerebras Llama 3.1 8B
The correct answer involves scoring AI responses based on correctness, penalising invented details and generic advice, and rewarding honesty when code is not available. The scoring rules are designed to prioritise accurate solutions over concise but incorrect ones

