# Session Log -- Mobius_Coder
_Written by Mobius_Coder. Read by Claude Desktop via MCP._

---

## Fri, 10 Apr 2026, 2:02 pm
**Q:** In js/commands.js the sendToAI function builds context before sending to the AI model. Show the exact code for how it calls getCodeContext and getMemoryContext, what it does with the results, and how it assembles the final prompt. Does it pass the brief to autoEvaluate?

**Brief injected (~2308 tokens):**
```
[Relevant files]
  [js/commands.js] js/commands.js

[Relevant code]
// js/commands.js -- sendToAI (line 183)
async function sendToAI(model, messages, output, outputEl, options = {}) {
  const userId = getAuth('mobius_user_id');
  const start  = Date.now();
  output('Thinking...');

  // Build context-prepended query: memory + brief + slim + last-read file + user query.
  // CLAUDE.md excluded -- contains MCP/Desktop instructions not relevant to cloud AI.
  let _finalQuery = messages[messages.length - 1].content;
  const _ctx   = window._projectContext;
  const _parts = [];
  if (window.getMemoryContext) {
    try {
      const _mem = await window.getMemoryContext(_finalQuery);
      if (_mem) _parts.push('[Memory]\n' + _mem);
    } catch { /* never block on memory */ }
  }
  if (window.getCodeContext) {
    try {
      const _code = await window.getCodeContext(_finalQuery);
      if (_code) _parts.push(_code);
    } catch { /* never block on code index */ }
  }
  if (_ctx) {
    if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
    if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
  }
  if (window.lastReadFile && window.lastReadFile.content) {
    const c = window.last
...[truncated]
```

### Cerebras Llama 3.1 8B
In `js/commands.js`, the `sendToAI` function builds context by calling `getCodeContext` and `getMemoryContext` in the following lines:

```javascript
// try to prep code context for the last user query
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// try to prep memory context for the last user query
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}
```

The `getCodeContext` and `getMemoryContext` functions are called with `_finalQuery` as an argument, which is the user's last message content. The results of these functions are appended to the `_parts` array. If the results are not empty, they are prepended to the `[_finalQuery]` to form the `_finalQuery` prompt.

Note that there is no mention of passing the brief to `autoEvaluate`. The `brief` property is appended to the `_parts` array like this:

```javascript
if (_ctx && _ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
```

This brief is prepended to the final prompt as part of the `_finalQuery` assembly process:

```javascript
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

This means that the brief is passed as part of the context to the AI model, but not evaluated by the `autoEvaluate` function. If you want to pass the brief for evaluation by `autoEvaluate`, you would need to modify the code accordingly.

### Groq Llama 3.3 70B
The `sendToAI` function in `js/commands.js` builds context by calling `getCodeContext` and `getMemoryContext` as follows:

```javascript
// Build context-prepended query: memory + brief + slim + last-read file + user query.
let _finalQuery = messages[messages.length - 1].content;
const _ctx   = window._projectContext;
const _parts = [];
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}
if (window.lastReadFile && window.lastReadFile.content) {
  const c = window.lastReadFile.content;
  _parts.push('[File: ' + window.lastReadFile.path + ']\n'
    + (c.length > 8000 ? c.slice(0, 8000) + '\n...[truncated]' : c));
}
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

It calls `getCodeContext` and `getMemoryContext` with the user's query as an argument. If the results are not empty, it prepends them to the `_parts` array with corresponding labels (`[Memory]` and no specific label for code). The project brief and slim are also added to the `_parts` array if available.

The final prompt (`_finalQuery`) is then assembled by joining all the context parts with newline characters (`\n\n`) and appending the user's query.

The brief is not passed to `autoEvaluate`; it is simply prepended to the query alongside other context information. There is no `autoEvaluate` function call in the provided code.

### Groq Llama 3.3 70B (fallback from Gemini)
The `sendToAI` function in `js/commands.js` calls `getCodeContext` and `getMemoryContext` as follows:

```javascript
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}
```

The results from `getCodeContext` and `getMemoryContext` are prepended to the `_parts` array if they are not empty. 

The brief is not passed to `autoEvaluate`. Instead, it is prepended to the final prompt if it exists in the project context:

```javascript
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}
```

The final prompt is assembled by joining the `_parts` array with the `_finalQuery`:

```javascript
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

This means that the context (including the brief, code context, and memory context) is prepended to the final query before it is sent to the AI model. 

Note that the results from `getCodeContext` and `getMemoryContext` are wrapped in headers (`[Memory]` and no header for code context) to distinguish them from the rest of the prompt.

### Codestral
Here's the exact code for how `sendToAI` calls `getCodeContext` and `getMemoryContext`, processes the results, and assembles the final prompt:

```javascript
// Memory context
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

// Code context
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// Brief and slim context
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}

// Final prompt assembly
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

Key points:
1. Both `getMemoryContext` and `getCodeContext` are called with the final query as input
2. Results are wrapped in markdown-style headers (`[Memory]`, `[Files]`, etc.)
3. All context parts are joined with double newlines
4. The original query is appended at the end
5. The brief is included in the context but not passed to `autoEvaluate` (that function isn't shown in the code)

### OpenRouter: lfm-2.5-1.2b-thinking-20260120
Yes, the brief is integrated into autoEvaluate.

---

## Fri, 10 Apr 2026, 2:03 pm
**Q:** [Brief AI -- Step 1/1]

**Brief AI:** STEP 1/1
WINNER: Cerebras Llama 3.1 8B (high)
REASON: This model provided an accurate and relevant response that directly answers the query.
SYNTHESIS: The sendToAI function in js/commands.js builds context by calling getCodeContext and getMemoryContext, and assembles the final prompt by prepending the context to the user's query. The brief is included in the context but not passed to autoEvaluate.

RANKINGS (A=accuracy H=hallucination R=relevance S=specificity, max 20):
1. Cerebras Llama 3.1 8B [total:20/20  A:5 H:5 R:5 S:5]
   Accurate and relevant response.
2. Groq Llama 3.3 70B [total:20/20  A:5 H:5 R:5 S:5]
   Accurate and relevant response.
3. Groq Llama 3.3 70B (fallback from Gemini) [total:20/20  A:5 H:5 R:5 S:5]
   Accurate and relevant response.
4. Codestral [total:20/20  A:5 H:5 R:5 S:5]
   Accurate and relevant response.
5. OpenRouter: lfm-2.5-1.2b-thinking-20260120 [total:4/20  A:1 H:1 R:1 S:1]  HALLUCINATED: autoEvaluate()
   Incorrect response.


