# Session Log -- Mobius_Coder
_Written by Mobius_Coder. Read by Claude Desktop via MCP._

---

## Mon, 13 Apr 2026, 10:14 pm
**Q:** Project: Brief -- generated for GPRTool

**Gemini:** # GPRTool -- Project Brief

## What this is
GPRTool is a web-based 3D visualisation application built with HTML, CSS, JavaScript, and Three.js, designed for processing and displaying ground-penetrating radar data with geospatial and plant information. It operates as a client-side web application, utilising a Python-based pipeline for backend processes like Leaf Area Index (LAI) calculations.

## Deployment
- Live URL: [FILL IN]
- Vercel project: [FILL IN]
- Supabase project: [FILL IN]
- Key env vars: [FILL IN e.g. SUPABASE_URL, SUPABASE_KEY, API keys]

## Current focus
The current development actively focuses on refactoring `index.html` by incrementally extracting its content into distinct, modular JavaScript files within the `app/js/` directory. The primary objective is to reduce `index.html` to a minimal shell that imports these modules.

## Key constraints
- Development and testing must be performed using Microsoft Edge due to known Chrome performance issues.
- App code within `app/` is strictly HTML, CSS, and JavaScript; Python is confined to the LAI pipeline and backend.
- JavaScript modules in `app/js/` must adhere to ES module rules, using imports and exports rather than global variables.
- All file operations must utilise MCP filesystem tools, not bash commands.
- Icons are to be implemented as inline SVG elements, never `<img>` tags referencing PNG files.

## Off-limits
- [FILL IN: data or files that must never be sent to external APIs or logged]

## Known issues
(none yet)

---TEMPLATE---
You are a coding assistant for GPRTool (browser PWA, vanilla JS, Vercel).
Source truth: treat injected [Relevant code] chunks as ground truth -- never invent function or variable names not shown there.
Answer format: always reference the exact file path and function name. For fixes, show the exact lines to change.
Task routing: Fix/Debug -> propose exact line diffs with file path. Explain -> cite specific functions from source. Plan -> reference existing architecture.
Runtime check: any response using require(), TypeScript syntax, or npm imports is wrong for this project.

---

## Mon, 13 Apr 2026, 10:14 pm
**Q:** Project: Setup -- GPRTool

**Coder:** 
â”€â”€ Setup complete for GPRTool â”€â”€
  OK   Map:
  OK   Project: Slim
  OK   Project: Brief
  OK   Project: Parse

