# Session Log -- Mobius_Coder
_Written by Mobius_Coder. Read by Claude Desktop via MCP._

---

## Fri, 10 Apr 2026, 3:07 pm
**Q:** In js/commands.js the sendToAI function builds context before sending to the AI model. Show the exact code for how it calls getCodeContext and getMemoryContext, what it does with the results, and how it assembles the final prompt. Does it pass the brief to autoEvaluate?

**Brief injected (~1586 tokens):**
```
[Relevant files]
  [js/commands.js] js/commands.js
  [api/auth/[service].js] api/auth/[service].js

[Relevant code]
// js/commands.js -- sendToAI (line 183)
async function sendToAI(model, messages, output, outputEl, options = {}) {
  const userId = getAuth('mobius_user_id');
  const start  = Date.now();
  output('Thinking...');

  // Build context-prepended query: memory + brief + slim + last-read file + user query.
  // CLAUDE.md excluded -- contains MCP/Desktop instructions not relevant to cloud AI.
  let _finalQuery = messages[messages.length - 1].content;
  const _ctx   = window._projectContext;
  const _parts = [];
  if (window.getMemoryContext) {
    try {
      const _mem = await window.getMemoryContext(_finalQuery);
      if (_mem) _parts.push('[Memory]\n' + _mem);
    } catch { /* never block on memory */ }
  }
  if (window.getCodeContext) {
    try {
      const _code = await window.getCodeContext(_finalQuery);
      if (_code) _parts.push(_code);
    } catch { /* never block on code index */ }
  }
  if (_ctx) {
    if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
    if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
  }
  if (window.lastReadFile && window.l
...[truncated]
```

### Cerebras Llama 3.1 8B
Here are the exact code snippets:

```javascript
// Calling getCodeContext
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// Calling getMemoryContext
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}
```

The results of `getCodeContext` and `getMemoryContext` (if successful) are appended to an array `_parts`. Then, the entire `_parts` array is joined with an empty string (`\n\n`) as a delimiter, and the resulting string is prepended to the final query (`_finalQuery`). However, it actually looks like the brief from the project context is already preprended to the query by `buildContext()` in `all.js`, as mentioned in the memory:

```javascript
[mobius] Mobius_Coder All Mode prepends brief + slim + lastReadFile content to every model query via buildContext() in all.js
```

The brief from the project context is not passed to `autoEvaluate`. The auto-extraction of memory works via the `autoExtractMemory` function in `window`, which is called after the AI response has been received:

```javascript
if (window.autoExtractMemory) {
  window.autoExtractMemory(messages[messages.length - 1].content, reply);
}
```

### Codestral
Here's the exact code for how `sendToAI` calls `getCodeContext` and `getMemoryContext`, processes the results, and assembles the final prompt:

```javascript
// Memory context call
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

// Code context call
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// Context assembly
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}

// Final prompt construction
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

Key points:
1. Both `getMemoryContext` and `getCodeContext` are called with the final query as input
2. Results are wrapped in markdown-style headers (`[Memory]`, `[Files]`, etc.)
3. All context parts are joined with double newlines
4. The original query is appended at the end
5. No, it does not pass the brief to `autoEvaluate` - that happens after the AI response is received in the `window.autoExtractMemory` call

### Groq Llama 3.1 8B
The code is located on lines 196-226 in the `sendToAI` function in `js/commands.js`. Here's the relevant code:

```javascript
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}
```

For `getMemoryContext` and `getCodeContext`, it calls these functions with the final query `_finalQuery` as an argument, and then appends the results to the `_parts` array. The results are prepended to the final query with a label indicating whether it was a memory or code context.

The brief is not passed to `autoEvaluate`. However, `autoEvaluate` is called later in the code when `window.autoExtractMemory` is enabled. The brief is not explicitly used in this process, but it might be used indirectly in the evaluation logic.

Finally, the assembled final prompt is built by joining the `_parts` array with newline characters, and then prepending this to the final query `_finalQuery`.

```javascript
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

### Gemini 2.5 flash
The `sendToAI` function in `js/commands.js` builds context by calling `window.getMemoryContext` and `window.getCodeContext`, prepending their results, along with the project brief, slim context, and the last read file's content, to the user's query.

Here's the exact code for how it calls `getMemoryContext` and `getCodeContext`, what it does with the results, and how it assembles the final prompt:

```javascript
// js/commands.js -- sendToAI (line 183)
async function sendToAI(model, messages, output, outputEl, options = {}) {
  // ... (other code)

  let _finalQuery = messages[messages.length - 1].content;
  const _ctx   = window._projectContext;
  const _parts = [];
  if (window.getMemoryContext) {
    try {
      const _mem = await window.getMemoryContext(_finalQuery);
      if (_mem) _parts.push('[Memory]\n' + _mem);
    } catch { /* never block on memory */ }
  }
  if (window.getCodeContext) {
    try {
      const _code = await window.getCodeContext(_finalQuery);
      if (_code) _parts.push(_code);
    } catch { /* never block on code index */ }
  }
  if (_ctx) {
    if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
    if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
  }
  if (window.lastReadFile && window.lastReadFile.content) {
    const c = window.lastReadFile.content;
    _parts.push('[File: ' + window.lastReadFile.path + ']\n'
      + (c.length > 8000 ? c.slice(0, 8000) + '\n...[truncated]' : c));
  }
  if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;

  // ... (rest of the function)
}
```

**Explanation:**

1.  **`getMemoryContext` Call:**
    ```javascript
    if (window.getMemoryContext) {
      try {
        const _mem = await window.getMemoryContext(_finalQuery);
        if (_mem) _parts.push('[Memory]\n' + _mem);
      } catch { /* never block on memory */ }
    }
    ```
    If `window.getMemoryContext` exists, it is called with the current user query (`_finalQuery`). If it returns a non-empty result (`_mem`), it is prepended with `[Memory]\n` and added to the `_parts` array. Errors are caught silently to prevent blocking.

2.  **`getCodeContext` Call:**
    ```javascript
    if (window.getCodeContext) {
      try {
        const _code = await window.getCodeContext(_finalQuery);
        if (_code) _parts.push(_code);
      } catch { /* never block on code index */ }
    }
    ```
    Similarly, if `window.getCodeContext` exists, it is called with `_finalQuery`. If a non-empty result (`_code`) is returned, it is added to the `_parts` array. Errors are also caught silently.

3.  **Final Prompt Assembly:**
    ```javascript
    // ... (after adding memory, code, brief, slim, and last read file content to _parts)
    if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
    ```
    After collecting context from memory, code index, project brief (`_ctx.brief`), project slim context (`_ctx.slim`), and the `window.lastReadFile.content` (truncated if over 8000 characters), all elements in the `_parts` array are joined together with two newline characters (`\n\n`) as separators. This combined context string is then prepended, followed by another `\n\n`, to the original user query (`_finalQuery`), forming the complete prompt sent to the AI model.

**Passing brief to `autoEvaluate`:**

The `sendToAI` function **does not** pass the `brief` to `autoEvaluate`. The function calls `window.autoExtractMemory` *after* the AI response is received, passing the original user query and the AI's reply to it:

```javascript
if (window.autoExtractMemory) {
  window.autoExtractMemory(messages[messages.length - 1].content, reply);
}
```

### OpenRouter: gemma-3n-e4b-it
```javascript
async function sendToAI(model, messages, output, outputEl, options = {}) {
  // ... (previous code) ...

  if (window.getCodeContext) {
    try {
      const _code = await window.getCodeContext(_finalQuery);
      if (_code) _parts.push(_code);
    } catch { /* never block on code index */ }
  }
  if (window.getMemoryContext) {
    try {
      const _mem = await window.getMemoryContext(_finalQuery);
      if (_mem) _parts.push('[Memory]\n' + _mem);
    } catch { /* never block on memory */ }
  }

  // ... (rest of the code) ...
}
```

The `sendToAI` function calls `window.getCodeContext` and `window.getMemoryContext` within `try...catch` blocks.

- **`window.getCodeContext(_finalQuery)`:** This function is called to retrieve code context relevant to the current query (`_finalQuery`). If successful, the retrieved code context is added to the `_parts` array. Any errors during this process are caught but do not halt execution.
- **`window.getMemoryContext(_finalQuery)`:** This function is called to retrieve memory context relevant to the current query (`_finalQuery`). If successful, the retrieved memory context is prepended to the `_parts` array with the label "[Memory]". Any errors during this process are caught but do not halt execution.

The results of these calls (the code and memory context, if available) are added to the `_parts` array. These parts are then joined together with the original `_finalQuery` to form the final prompt sent to the AI model.

The code does **not** pass the `brief` to `autoEvaluate`. The `brief` is used to add project context to the prompt but is not explicitly passed to an `autoEvaluate` function in the provided code snippet.

---

## Fri, 10 Apr 2026, 3:08 pm
**Q:** [Brief AI -- Step 1/1]

**Brief AI:** STEP 1/1
WINNER: Codestral (high)
VOTES (4/5 evaluators): Codestral×3, Cerebras Llama 3.1 8B×1
REASON: Won by 3/4 evaluators
SYNTHESIS: The sendToAI function calls getMemoryContext and getCodeContext with the final query, wraps results in markdown headers, joins all parts with double newlines, and prepends to the original query. The brief is not passed to autoEvaluate.

RANKINGS (A=accuracy H=hallucination R=relevance S=specificity, averaged across evaluators, max 20):
1. Codestral [total:19/20  A:5 H:5 R:4.8 S:4.3 n=4 [self:5]]  HALLUCINATED: buildContext(), autoExtractMemory()
   [groq] Perfect match to source code, no hallucinations | [gemini] Perfect match to source code, no hallucinations
2. Gemini 2.5 flash [total:15.3/20  A:4.3 H:2.5 R:4.8 S:3.8 n=4 [self:4]]  HALLUCINATED: result(), characters(), autoExtractMemory(), none
   [groq] Correct code but includes unverified function calls | [gemini] Close but includes unverified calls
3. Groq Llama 3.1 8B [total:15.3/20  A:4.3 H:2.5 R:4.8 S:3.8 n=4 [self:4]]  HALLUCINATED: none
   [groq] Correct code but truncated response | [gemini] Good but truncated final assembly
4. OpenRouter: gemma-3n-e4b-it [total:14.7/20  A:3.7 H:2.7 R:5 S:3.3 n=3]  HALLUCINATED: calls(), none
   [groq] Correct code but includes unverified function calls | [codestral] Good but includes unverified function call
5. Cerebras Llama 3.1 8B [total:14.5/20  A:3.5 H:2.5 R:5 S:3.5 n=4 [self:5]]  HALLUCINATED: buildContext(), autoExtractMemory(), none
   [groq] Correct code but includes unverified function calls | [gemini] Mixes real and unverified calls
6. gemma-3n-e4b-it [total:14/20  A:3 H:3 R:5 S:3 n=1]  HALLUCINATED: calls()
   [gemini] Good but mentions unverified call


