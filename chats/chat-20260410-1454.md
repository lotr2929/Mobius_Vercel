# Session Log -- Mobius_Coder
_Written by Mobius_Coder. Read by Claude Desktop via MCP._

---

## Fri, 10 Apr 2026, 2:55 pm
**Q:** In js/commands.js the sendToAI function builds context before sending to the AI model. Show the exact code for how it calls getCodeContext and getMemoryContext, what it does with the results, and how it assembles the final prompt. Does it pass the brief to autoEvaluate?

**Brief injected (~2308 tokens):**
```
[Relevant files]
  [js/commands.js] js/commands.js

[Relevant code]
// js/commands.js -- sendToAI (line 183)
async function sendToAI(model, messages, output, outputEl, options = {}) {
  const userId = getAuth('mobius_user_id');
  const start  = Date.now();
  output('Thinking...');

  // Build context-prepended query: memory + brief + slim + last-read file + user query.
  // CLAUDE.md excluded -- contains MCP/Desktop instructions not relevant to cloud AI.
  let _finalQuery = messages[messages.length - 1].content;
  const _ctx   = window._projectContext;
  const _parts = [];
  if (window.getMemoryContext) {
    try {
      const _mem = await window.getMemoryContext(_finalQuery);
      if (_mem) _parts.push('[Memory]\n' + _mem);
    } catch { /* never block on memory */ }
  }
  if (window.getCodeContext) {
    try {
      const _code = await window.getCodeContext(_finalQuery);
      if (_code) _parts.push(_code);
    } catch { /* never block on code index */ }
  }
  if (_ctx) {
    if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
    if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
  }
  if (window.lastReadFile && window.lastReadFile.content) {
    const c = window.last
...[truncated]
```

### Cerebras Llama 3.1 8B
Here is the relevant code from `js/commands.js`:

```javascript
// ...

if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// ...
```

This code calls `getMemoryContext` with the `_finalQuery` and prepends the response to the `_parts` array if it's not `null`. It does the same for `getCodeContext`.

When assembling the final prompt, it checks if `_parts` is not empty and joins its elements with newline characters (\n\n), followed by the current `_finalQuery`.

Here's the specific code for assembling the final prompt:

```javascript
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

Finally, it uses the constructed `_finalQuery` to send a request to the AI model.

As for passing the brief to `autoEvaluate`, there's no clear evidence in the provided code that it's being passed to `autoEvaluate`.

### Groq Llama 3.3 70B
The exact code for calling `getCodeContext` and `getMemoryContext` in `sendToAI` function is as follows:

```javascript
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}
```

The results of `getCodeContext` and `getMemoryContext` are used to prepend context to the user's query. If the results are truthy, they are wrapped in a string with a label (`'[Memory]\n'` or no label for code context) and added to the `_parts` array.

The final prompt is assembled by joining all the context parts and the user's query:

```javascript
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

The brief is not passed to `autoEvaluate` in this code snippet. However, if a `_ctx` object with a `brief` property exists, the brief is prepended to the context:

```javascript
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  // ...
}
```

### Groq Llama 3.3 70B (fallback from Gemini)
The `sendToAI` function in `js/commands.js` calls `getCodeContext` and `getMemoryContext` as follows:

```javascript
let _finalQuery = messages[messages.length - 1].content;
const _ctx   = window._projectContext;
const _parts = [];
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}
if (window.lastReadFile && window.lastReadFile.content) {
  const c = window.lastReadFile.content;
  _parts.push('[File: ' + window.lastReadFile.path + ']\n'
    + (c.length > 8000 ? c.slice(0, 8000) + '\n...[truncated]' : c));
}
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

The function calls `getMemoryContext` and `getCodeContext` with the user's query as an argument, and if a result is returned, it prepends the result to the final query within a context label (e.g., `[Memory]`, `[Code]`, etc.). 

It does not pass the brief to `autoEvaluate`; instead, it appends the brief to the final query as part of the project context. 

The assembled final prompt includes the following elements:

1. Memory context (if available)
2. Code context (if available)
3. Project brief (if available)
4. Project file list (if available)
5. Last read file content (if available)
6. User's query

These elements are joined together with newline characters to form the final prompt sent to the AI model.

### Codestral
Here's the exact code for how `sendToAI` calls `getCodeContext` and `getMemoryContext`, processes the results, and assembles the final prompt:

```javascript
// Memory context call
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

// Code context call
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// Brief inclusion
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
}

// Final prompt assembly
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

Key points:
1. Both `getMemoryContext` and `getCodeContext` are called with the final query as input
2. Results are wrapped in markdown-style headers (`[Memory]`, `[Project]`, etc.)
3. All context parts are joined with double newlines
4. The original query is appended at the end
5. The brief is included directly from `_ctx.brief` (not passed to `autoEvaluate`)

The function does not explicitly pass the brief to `autoEvaluate` - it's included in the final prompt assembly.

### OpenRouter: nemotron-3-nano-30b-a3b
**Answer – how `sendToAI` builds the context**

```js
// 1️⃣  Get memory context (lines 37‑53)
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);   // ← calls the function    if (_mem) _parts.push('[Memory]\n' + _mem);                 // ← prepends it
  } catch (_) { /* memory lookup never blocks */ }
}

// 2️⃣  Get code context (lines 60‑73)
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);    // ← calls the function
    if (_code) _parts.push(_code);                             // ← prepends it
  } catch (_) { /* code lookup never blocks */ }
}

// 3️⃣  Add project brief / slim if they exist (lines 46‑50)
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);    // ← brief
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);     // ← slim}

// 4️⃣  Append the content of the file the user last opened
if (window.lastReadFile && window.lastReadFile.content) {
  const c = window.lastReadFile.content;
  _parts.push('[File: ' + window.lastReadFile.path + ']\n' +
      (c.length > 8000 ? c.slice(0, 8000) + '\n...[truncated]' : c));
}

// 5️⃣  Join everything and tack on the user’s last messageif (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

### What the code does with the results

| Step | What is fetched | What is done with the value |
|------|-----------------|-----------------------------|
| `getMemoryContext` | A string of *relevant memories* (usually a list of fact‑snippets) | If a string is returned it is **prepended** to `_parts` under the heading **[Memory]**. |
| `getCodeContext`   | A string that contains the *relevant source code* (e.g. a whole file or a snippet) | If a string is returned it is **prepended** to `_parts` without any extra heading (just the raw code). |
| `ctx.brief` / `ctx.slim` | Project‑level meta‑data (`brief` = short summary, `slim` = condensed file list) | When they exist they are **prepended** under **[Project]** or **[Files]** respectively. |
| `lastReadFile` | The full content of the last file the user opened | The whole content (up‑to‑8 k chars) is wrapped in a **[File: …]** heading and appended to `_parts`. |

All the pieces collected in `_parts` are **joined with double‑newlines** (`'\n\n'`) so each section is clearly separated, and then the original user query (`_finalQuery`) is appended at the end. The resulting string (`_finalQuery`) becomes the **final prompt** sent to the AI.

### Does it pass the brief to `autoEvaluate`?

No. The code builds a `_parts` array that contains `'[Project]\n' + _ctx.brief` (or also `'[Files]\n' + _ctx.slim`) when those fields exist, and those items are simply concatenated into `_finalQuery`.  
There is **no explicit call to a function named `autoEvaluate`** inside `sendToAI`. The brief is therefore only used to enrich the prompt; it is not handed off to a separate evaluation routine within this function. If downstream code (e.g. the API handler on the server) later decides to perform evaluation, that logic would be outside the scope of `sendToAI`.

---

## Fri, 10 Apr 2026, 2:55 pm
**Q:** [Brief AI -- Step 1/1]

**Brief AI:** STEP 1/1
WINNER: Groq Llama 3.3 70B (high)
REASON: Most accurate with complete context-building logic and no hallucinations
SYNTHESIS: The function calls getMemoryContext and getCodeContext with the final query, prepends results with labels, includes brief and slim from _ctx, adds lastReadFile content (truncated if needed), and joins all parts with double newlines before the query. The brief is included in context but not passed to autoEvaluate.

RANKINGS (A=accuracy H=hallucination R=relevance S=specificity, max 20):
1. Groq Llama 3.3 70B [total:20/20  A:5 H:5 R:5 S:5]
   Accurately describes all context-building logic and final prompt assembly
2. Groq Llama 3.3 70B (fallback from Gemini) [total:18/20  A:4 H:5 R:5 S:4]
   Correct but slightly truncated in final prompt assembly
3. Codestral [total:18/20  A:4 H:5 R:5 S:4]
   Accurate but omits lastReadFile inclusion in context
4. OpenRouter: nemotron-3-nano-30b-a3b [total:16/20  A:4 H:3 R:5 S:4]  HALLUCINATED: exist(), messageif(), heading()
   Correct but includes unverified function calls
5. Cerebras Llama 3.1 8B [total:14/20  A:3 H:3 R:5 S:3]  HALLUCINATED: characters()
   Misses lastReadFile inclusion and includes unverified call


