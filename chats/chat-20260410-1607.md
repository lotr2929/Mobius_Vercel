# Session Log -- Mobius_Coder
_Written by Mobius_Coder. Read by Claude Desktop via MCP._

---

## Fri, 10 Apr 2026, 4:07 pm
**Q:** In js/commands.js the sendToAI function builds context before sending to the AI model. Show the exact code for how it calls getCodeContext and getMemoryContext, what it does with the results, and how it assembles the final prompt. Does it pass the brief to autoEvaluate?

**Brief injected (~1586 tokens):**
```
[Relevant files]
  [js/commands.js] js/commands.js
  [api/auth/[service].js] api/auth/[service].js

[Relevant code]
// js/commands.js -- sendToAI (line 183)
async function sendToAI(model, messages, output, outputEl, options = {}) {
  const userId = getAuth('mobius_user_id');
  const start  = Date.now();
  output('Thinking...');

  // Build context-prepended query: memory + brief + slim + last-read file + user query.
  // CLAUDE.md excluded -- contains MCP/Desktop instructions not relevant to cloud AI.
  let _finalQuery = messages[messages.length - 1].content;
  const _ctx   = window._projectContext;
  const _parts = [];
  if (window.getMemoryContext) {
    try {
      const _mem = await window.getMemoryContext(_finalQuery);
      if (_mem) _parts.push('[Memory]\n' + _mem);
    } catch { /* never block on memory */ }
  }
  if (window.getCodeContext) {
    try {
      const _code = await window.getCodeContext(_finalQuery);
      if (_code) _parts.push(_code);
    } catch { /* never block on code index */ }
  }
  if (_ctx) {
    if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
    if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
  }
  if (window.lastReadFile && window.l
...[truncated]
```

### Cerebras
The code that calls `getCodeContext` and `getMemoryContext` is as follows:

```javascript
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}
```

Here, `_finalQuery` is the last sent question (`messages[messages.length - 1].content`) prepended with any context that has been gathered from `memory` and `code`.

The results of `getCodeContext` and `getMemoryContext` are stored in the `_parts` array, which contains a list of possible context information to be prepended to the `_finalQuery`.

To assemble the final question, the code does the following:

1. It joins the `_parts` array into a single string with newline characters in between:

```javascript
_finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

This will result in the context information being prepended to the query with a newline after each piece of context, and another newline before the final query.

As for whether the brief is passed to `autoEvaluate`, the brief is not explicitly passed to any function after it is loaded from the context via `buildContext()`. However, the brief is used in the assembly of the final query and is stored in the `_ctx` object.

In the case where `autoExtractMemory()` is being used, it will operate on the entire conversation history including the project brief. 

Here's an excerpt from the memory system section in your [memory] log:

```javascript
[mobius] Mobius_Coder memory injection: getMemoryContext() called before every AI query -- top 15 semantically relevant memories prepended to system context
[mobius] Mobius_Coder memory system auto-extracts facts after every AI response via autoExtractMemory() -- writes Q+A pair to memory_general for later distillation
```

This indicates that the memory system is aware of the project brief through the `getMemoryContext()` function and the project brief is used in the AI query assembly. The brief itself is not explicitly passed to `autoExtractMemory()` but is stored in the conversation history.

If you would like more information or insight, don't hesitate to ask.

### Groq
### getCodeContext and getMemoryContext Calls

The `sendToAI` function in `js/commands.js` calls `getCodeContext` and `getMemoryContext` as follows:

```javascript
// Build context-prepended query: memory + brief + slim + last-read file + user query.
let _finalQuery = messages[messages.length - 1].content;
const _ctx   = window._projectContext;
const _parts = [];
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}
```

### Processing Results and Assembling the Final Prompt

The results from `getCodeContext` and `getMemoryContext` are processed and assembled into the final prompt as follows:

```javascript
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}
if (window.lastReadFile && window.lastReadFile.content) {
  const c = window.lastReadFile.content;
  _parts.push('[File: ' + window.lastReadFile.path + ']\n'
    + (c.length > 8000 ? c.slice(0, 8000) + '\n...[truncated]' : c));
}
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

The brief is not passed to `autoEvaluate` in this code snippet. Instead, it is prepended to the final query along with other context information.

The final assembled prompt is then sent to the AI model using the `fetch` API:

```javascript
const res = await fetch('/ask', {
  method:  'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    query:   _finalQuery,
    model,
    userId,
    history: [...conversationHistory, ...messages.slice(0, -1)]
  })
});
```

### OpenRouter
[Error: HTTP 500]

### Mistral
Here's the exact code for how `sendToAI` calls `getCodeContext` and `getMemoryContext`, processes their results, and assembles the final prompt:

```javascript
// Memory context
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

// Code context
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// Project context
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}

// Final prompt assembly
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

Key points:
1. Both `getMemoryContext` and `getCodeContext` are called with the final query as input
2. Their results are wrapped in markdown-style headers (`[Memory]` and `[Code]`)
3. All context parts are collected in the `_parts` array
4. The final prompt is assembled by joining all parts with double newlines, followed by the original query
5. The brief is included in the context but not passed directly to `autoEvaluate` - it's part of the assembled prompt sent to the AI model

The memory and code contexts are prepended to the user's query before being sent to the AI model, while the brief is included as part of the project context.

### QwQ
The code that calls `getCodeContext` and `getMemoryContext` is located in the `js/commands.js` file and within the `sendToAI` function, specifically at lines 187-202.

```javascript
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}
```

Here's a step-by-step explanation of what the code does:

1. `getCodeContext` and `getMemoryContext` are called with `_finalQuery` as the input parameter.
2. The results of these function calls are stored in `_code` and `_mem` variables, respectively.
3. If `_code` or `_mem` is truthy (i.e., not `null` or `undefined`), they are prepended to the `_parts` array with a header indicating whether it's code or memory data.

The final prompt is assembled by joining the `_parts` array (which now contains the prepended data) with `\n\n` in between each part, and then appending the original `_finalQuery` at the end. Here's the assembled code:

```javascript
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

This final prompt is then sent to the AI model in the `fetch` request:

```javascript
body: JSON.stringify({
  query: _finalQuery,
  model,
  userId,
  history: [...conversationHistory, ...messages.slice(0, -1)]
})
```

The `autoExtractMemory` function is called with the last AI response and the current user query:

```javascript
if (window.autoExtractMemory) {
  window.autoExtractMemory(messages[messages.length - 1].content, reply);
}
```

As for your question about whether the brief is passed to `autoEvaluate`, the code does not explicitly pass the brief to `autoEvaluate`. The memory injection system prepends the brief to the query via `getMemoryContext`, but this is not the same as passing the brief to `autoEvaluate`. To pass the brief to `autoEvaluate`, you would need to modify the code to explicitly call `autoEvaluate` with the brief as an argument.

### DeepSeek R1
Here's the exact code for how `sendToAI` calls `getCodeContext` and `getMemoryContext`, processes their results, and assembles the final prompt:

```javascript
// Memory context call
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

// Code context call
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// Final prompt assembly
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

Key points:
1. Both calls are wrapped in try-catch blocks to prevent blocking
2. Results are pushed to `_parts` array with appropriate section headers
3. The final prompt is assembled by joining all parts with double newlines
4. The original user query is appended at the end

The code does not pass the brief to `autoEvaluate` - that happens later in the function when calling `window.autoExtractMemory()`. The brief is included in the context but not directly evaluated at this stage.

### DeepSeek V3
Here's the exact code for how `sendToAI` calls `getCodeContext` and `getMemoryContext`, processes their results, and assembles the final prompt:

```javascript
// Memory context
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

// Code context
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// Project context
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}

// Last read file
if (window.lastReadFile && window.lastReadFile.content) {
  const c = window.lastReadFile.content;
  _parts.push('[File: ' + window.lastReadFile.path + ']\n'
    + (c.length > 8000 ? c.slice(0, 8000) + '\n...[truncated]' : c));
}

// Assemble final query
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

Key points:
1. Both `getMemoryContext` and `getCodeContext` are called with the final query as input
2. Their results are wrapped in section headers (`[Memory]`, `[Files]`, etc.)
3. All context parts are joined with double newlines
4. The original query is appended at the end
5. The brief is included in the context but not passed directly to `autoEvaluate` - it's part of the assembled prompt

The brief is included in the context but not passed separately to `autoEvaluate` - it's part of the assembled prompt that gets sent to the AI model.

### Gemini
Here's the exact code for how `sendToAI` calls `getCodeContext` and `getMemoryContext`, processes their results, and assembles the final prompt:

```javascript
// Memory context call
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

// Code context call
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// Context assembly
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}

// Final prompt construction
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

Key points:
1. Both `getMemoryContext` and `getCodeContext` are called with the final query as input
2. Results are wrapped in markdown-style headers (`[Memory]`, `[Files]`, etc.)
3. All context parts are joined with double newlines
4. The final prompt is constructed by concatenating all context parts with the original query
5. The brief is included in the context assembly but not passed directly to `autoEvaluate` (that happens later in the flow via `appendToLog` and `autoExtractMemory`)

### GitHub
Here’s the precise breakdown of how the `sendToAI` function in `js/commands.js` calls `getCodeContext` and `getMemoryContext`, processes their results, and assembles the final prompt:

### 1. **Calling `getMemoryContext`**
The function checks if `window.getMemoryContext` exists. If so, it calls it asynchronously with the user-supplied query (`_finalQuery`), wrapped in a `try-catch` block to prevent blocking on failure. If `getMemoryContext` returns a result, it prepends it to the `_parts` array:
```javascript
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}
```

### 2. **Calling `getCodeContext`**
Similarly, the function checks for `window.getCodeContext`. If available, it invokes it asynchronously with `_finalQuery`, also wrapped in a `try-catch`. The result, if any, is added to the `_parts` array:
```javascript
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}
```

### 3. **Assembling the Final Prompt**
- `_parts` serves as a container for the context.
- It appends:
  - Memory context from `getMemoryContext` (if available).
  - Code context from `getCodeContext` (if available).
  - Project brief (`[Project]\n`), file list (`[Files]\n`), and the content of the last-read file, which are also included as part of `_ctx`.
- Finally, these components are concatenated using `\n\n`, followed by the user's query (`_finalQuery`). The resulting string becomes the final context-prepended query:
```javascript
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n' + _ctx.slim);
}
if (window.lastReadFile && window.lastReadFile.content) {
  const c = window.lastReadFile.content;
  _parts.push('[File: ' + window.lastReadFile.path + ']\n'
    + (c.length > 8000 ? c.slice(0, 8000) + '\n...[truncated]' : c));
}
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

### 4. **Does It Pass the Brief to `autoExtractMemory`?**
No, there is no explicit code that passes the project brief (`_ctx.brief`) or any part of `_finalQuery` to `autoExtractMemory`. The `autoExtractMemory` function is only passed the user's query (`messages[messages.length - 1].content`) and the AI response (`reply`):
```javascript
if (window.autoExtractMemory) {
  window.autoExtractMemory(messages[messages.length - 1].content, reply);
}
``` 

### Summary
- `getMemoryContext` and `getCodeContext` are called before every query, and their results (if any) are appended to `_parts` for context.
- The brief is loaded as part of `_ctx`, appended to `_parts`, and included in the final query.
- `autoExtractMemory` does **not** directly process the brief—it works solely with the user's query and the AI's reply.

---

## Fri, 10 Apr 2026, 4:08 pm
**Q:** [Brief AI -- Step 1/1]

**Brief AI:** STEP 1/1
WINNER: DeepSeek R1 (high)
TIMING: Task: 7.1s | Eval: 8.7s | Total: 15.8s
VOTES (7/9 evaluators): DeepSeek R1×4, Groq×1, Cerebras×1, DeepSeek V3×1
REASON: Score 18.7/20 · 4/9 evaluators voted
SYNTHESIS: The sendToAI function calls getCodeContext and getMemoryContext with the final query, wraps results in headers, and joins all parts with double newlines before the original query. The brief is included in context but not passed to autoEvaluate.

RANKINGS (A=accuracy H=hallucination R=relevance S=specificity, averaged across evaluators, max 20):
1. DeepSeek R1 [total:18.7/20  A:4.7 H:4.3 R:5 S:4.7 n=7 [self:5]]
   [groq] Precisely matches source code, no hallucinations, and answers the query directly | [gemini] Precisely matches source code for context calls and assembly
2. DeepSeek V3 [total:18.3/20  A:4.4 H:4.3 R:5 S:4.6 n=7 [self:4]]
   [groq] Mostly accurate but slightly truncated in final assembly | [gemini] Comprehensive and accurate with full context assembly
3. Gemini [total:17.6/20  A:4.1 H:4.3 R:5 S:4.1 n=7 [self:4]]
   [groq] Accurate but truncated the final assembly code | [gemini] Accurate but slightly truncated in final assembly
4. Mistral [total:17.6/20  A:4.1 H:4.3 R:5 S:4.1 n=7 [self:4]]
   [groq] Accurate but truncated the final assembly code | [gemini] Accurate but slightly truncated in final assembly
5. GitHub [total:17.1/20  A:4 H:4.1 R:5 S:4 n=7 [self:4]]
   [groq] Accurate but truncated the final assembly code | [gemini] Accurate but slightly truncated in final assembly
6. Groq [total:16.6/20  A:3.9 H:4.3 R:4.7 S:3.7 n=7 [self:4]]
   [groq] Accurate but truncated the final assembly code | [gemini] Accurate but slightly truncated in final assembly
7. Cerebras [total:15.7/20  A:3.4 H:3.7 R:5 S:3.6 n=7 [self:5]]  HALLUCINATED: question()
   [groq] Partially correct but omits project context and brief handling | [gemini] Correct but uses unverified terms
8. QwQ [total:12.8/20  A:2.5 H:2.8 R:4.7 S:2.8 n=6]  HALLUCINATED: truthy(), array()
   [groq] Invents JavaScript concepts not in source code | [gemini] Correct but uses unverified terms

FAILED:
- OpenRouter: [Error: HTTP 500]


