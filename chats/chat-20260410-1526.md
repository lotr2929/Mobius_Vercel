# Session Log -- Mobius_Coder
_Written by Mobius_Coder. Read by Claude Desktop via MCP._

---

## Fri, 10 Apr 2026, 3:27 pm
**Q:** In js/commands.js the sendToAI function builds context before sending to the AI model. Show the exact code for how it calls getCodeContext and getMemoryContext, what it does with the results, and how it assembles the final prompt. Does it pass the brief to autoEvaluate?

**Brief injected (~1586 tokens):**
```
[Relevant files]
  [js/commands.js] js/commands.js
  [api/auth/[service].js] api/auth/[service].js

[Relevant code]
// js/commands.js -- sendToAI (line 183)
async function sendToAI(model, messages, output, outputEl, options = {}) {
  const userId = getAuth('mobius_user_id');
  const start  = Date.now();
  output('Thinking...');

  // Build context-prepended query: memory + brief + slim + last-read file + user query.
  // CLAUDE.md excluded -- contains MCP/Desktop instructions not relevant to cloud AI.
  let _finalQuery = messages[messages.length - 1].content;
  const _ctx   = window._projectContext;
  const _parts = [];
  if (window.getMemoryContext) {
    try {
      const _mem = await window.getMemoryContext(_finalQuery);
      if (_mem) _parts.push('[Memory]\n' + _mem);
    } catch { /* never block on memory */ }
  }
  if (window.getCodeContext) {
    try {
      const _code = await window.getCodeContext(_finalQuery);
      if (_code) _parts.push(_code);
    } catch { /* never block on code index */ }
  }
  if (_ctx) {
    if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
    if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
  }
  if (window.lastReadFile && window.l
...[truncated]
```

### Groq Llama 3.3 70B
The `sendToAI` function in `js/commands.js` calls `getCodeContext` and `getMemoryContext` as follows:

```javascript
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}
```

The results from these context functions are then used to assemble the final prompt by pushing them onto the `_parts` array. 

After calling `getCodeContext` and `getMemoryContext`, it also adds the brief and slim project context if available:

```javascript
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}
```

Additionally, it appends the content of the last read file:

```javascript
if (window.lastReadFile && window.lastReadFile.content) {
  const c = window.lastReadFile.content;
  _parts.push('[File: ' + window.lastReadFile.path + ']\n'
    + (c.length > 8000 ? c.slice(0, 8000) + '\n...[truncated]' : c));
}
```

Finally, it assembles the final prompt by joining all the parts together and appending the original query:

```javascript
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

The brief is not passed to `autoEvaluate`. Instead, it is prepended to the query as part of the project context. The assembled final prompt is then sent to the AI model via a fetch request.

### Cerebras Llama 3.1 8B
The specific lines of code you requested from the `sendToAI` function in `js/commands.js` are:

```javascript
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}
```

However, let's also include the lines that append the brief and slim from the project context:

```javascript
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}
```

And the lines that append the last read file content (if any):

```javascript
if (window.lastReadFile && window.lastReadFile.content) {
  const c = window.lastReadFile.content;
  _parts.push('[File: ' + window.lastReadFile.path + ']\n'
    + (c.length > 8000 ? c.slice(0, 8000) + '\n...[truncated]' : c));
}
```

Lastly, the final prompt is assembled by joining these parts:

```javascript
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

This new query with prepended context will then be sent to the AI model.

It does not explicitly pass the brief to autoEvaluate. However, if the brief is a part of `_ctx` and is appended to `_parts`, the AI model will receive this information as part of the query.

As for `autoExtractMemory` not being called with the brief:

```javascript
if (window.autoExtractMemory) {
  window.autoExtractMemory(messages[messages.length - 1].content, reply);
}
```

This suggests that the brief is not passed as an input to `autoExtractMemory`, but rather the final query content (`messages[messages.length - 1].content`) is used as a parameter.

### Mistral Codestral (fallback from Groq Llama 3.3 70B) (fallback from Gemini)
Here's the exact code for how `sendToAI` calls `getCodeContext` and `getMemoryContext`, processes their results, and assembles the final prompt:

```javascript
// Memory context call
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

// Code context call
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// Brief and slim context
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}

// Final prompt assembly
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

Key points:
1. Both `getMemoryContext` and `getCodeContext` are called with the final query as input
2. Results are wrapped in markdown-style headers (`[Memory]`, `[Files]`, etc.)
3. All context parts are collected in `_parts` array
4. The final prompt is assembled by joining all parts with double newlines, then appending the original query
5. The brief is included in the context but not passed directly to `autoEvaluate` - it's part of the assembled prompt sent to the AI model

### Codestral
Here's the exact code for how `sendToAI` calls `getCodeContext` and `getMemoryContext`, processes the results, and assembles the final prompt:

```javascript
// Memory context call
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

// Code context call
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// Context assembly
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

Key points:
1. Both functions are called with the final query as parameter
2. Results are pushed to `_parts` array with appropriate section headers
3. All parts are joined with double newlines before the final query
4. The brief is included later in the context assembly:
   ```javascript
   if (_ctx) {
     if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
     if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
   }
   ```
5. The brief is not passed directly to `autoEvaluate` - it's included in the context sent to the AI model. The auto-evaluation happens after the response is received:
   ```javascript
   if (window.autoExtractMemory) {
     window.autoExtractMemory(messages[messages.length - 1].content, reply);
   }
   ```

### OpenRouter: minimax-m2.5-20260211
## Analysis of sendToAI Context Building

### Calls to getMemoryContext and getCodeContext

```javascript
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}
```

- Both are optional (`window.getMemoryContext` / `window.getCodeContext` guards)
- Both are awaited and called with `_finalQuery` (the user's raw query)
- Both wrapped in try/catch that silently swallows errors
- Results are pushed to `_parts` array with headers

### Final Prompt Assembly

```javascript
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

**Order in final prompt:**
1. Memory context (semantic search results)
2. Code context (codebase index results)
3. Project brief (`_ctx.brief`)
4. Project slim file list (`_ctx.slim`)
5. Last-read file content (truncated to 8000 chars)
6. **User query** (appended at the end)

### Does it pass brief to autoEvaluate?

**No.** The brief is used only for the AI prompt context. Looking at the code, `autoExtractMemory` is called with the **user query and AI reply**:

```javascript
if (window.autoExtractMemory) {
  window.autoExtractMemory(messages[messages.length - 1].content, reply);
}
```

The brief is **not** passed to `autoExtractMemory` — it goes only to the AI as context.

---

## Fri, 10 Apr 2026, 3:27 pm
**Q:** [Brief AI -- Step 1/1]

**Brief AI:** STEP 1/1
WINNER: Groq Llama 3.3 70B (high)
TIMING: Task: 46.1s | Eval: 25.0s | Total: 71.1s
VOTES (4/5 evaluators): Groq Llama 3.3 70B×3, Mistral Codestral (fallback from Groq Llama 3.3 70B) (fallback from Gemini)×1
REASON: Won by 3/4 evaluators
SYNTHESIS: The sendToAI function calls getMemoryContext and getCodeContext with the final query, wraps results in headers, and assembles the prompt by joining all context parts with the original query. The brief is included in context but not passed directly to autoEvaluate.

RANKINGS (A=accuracy H=hallucination R=relevance S=specificity, averaged across evaluators, max 20):
1. Cerebras Llama 3.1 8B [total:18.5/20  A:5 H:3.5 R:5 S:5 n=4 [self:5]]
   [groq] Also matches source code exactly with precise variable references | [codestral] Perfect match to source code, no hallucinations
2. OpenRouter: minimax-m2.5-20260211 [total:18.3/20  A:5 H:3.3 R:5 S:5 n=3]
   [groq] Precisely matches source code with variable names and context order | [codestral] Perfect match to source code, no hallucinations
3. Mistral Codestral (fallback from Groq Llama 3.3 70B) (fallback from Gemini) [total:17.9/20  A:4.7 H:3.6 R:4.9 S:4.7 n=7 [self:5]]  HALLUCINATED: autoEvaluate
   [groq] Correctly describes all context-building steps with source code references | [groq] Accurately quotes source code with variable names and logic
4. Groq Llama 3.3 70B [total:17.8/20  A:5 H:2.8 R:5 S:5 n=5 [self:5]]
   [groq] Perfectly matches source code with exact variable names and logic | [codestral] Perfect match to source code, no hallucinations


