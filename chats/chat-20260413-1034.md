# Session Log -- Mobius_Coder
_Written by Mobius_Coder. Read by Claude Desktop via MCP._

---

## Mon, 13 Apr 2026, 10:34 am
**Q:** Project: Brief -- generated for GPRTool

**Gemini:** # GPRTool -- Project Brief

## What this is
GPRTool is a web-based 3D visualisation and modelling application, likely for geospatial or GPR data, developed by Boon Ong at Curtin University. It utilises HTML, CSS, JavaScript (ESM modules) with Three.js for the frontend, and Python for backend processing (LAI pipeline). Development and testing are specifically conducted in Microsoft Edge.

## Deployment
- Live URL: [FILL IN]
- Vercel project: [FILL IN]
- Supabase project: [FILL IN]
- Key env vars: [FILL IN e.g. SUPABASE_URL, SUPABASE_KEY, API keys]

## Current focus
The main current focus is refactoring `index.html` by incrementally extracting its functional sections into modular JavaScript files within `app/js/`. This aims to reduce `index.html` to a minimal shell, establish a well-defined `app/js/` module structure, and keep `_map.md` as the authoritative file structure record.

## Key constraints
- Use Microsoft Edge for all development and testing due to known Chrome issues.
- `app/` directory must only contain HTML, CSS, JavaScript; `lai/` must only contain Python.
- All icons must be inline SVG; raster images are restricted to `app/images/`.
- All `app/js/` modules must declare dependencies at the top and expose only what is strictly necessary.
- Any modification to `index.html` mandates extracting the affected code into a new `app/js/` module.
- File operations must exclusively use MCP filesystem tools, not bash commands.
- Prioritise the simplest solution that aligns with existing logic and structure, aiming for minimal files, dependencies, and moving parts.

## Off-limits
- [FILL IN: data or files that must never be sent to external APIs or logged]

## Known issues
(none yet)

---TEMPLATE---
You are a coding assistant for GPRTool (browser PWA, vanilla JS, Vercel).
Source truth: treat injected [Relevant code] chunks as ground truth -- never invent function or variable names not shown there.
Answer format: always reference the exact file path and function name. For fixes, show the exact lines to change.
Task routing: Fix/Debug -> propose exact line diffs with file path. Explain -> cite specific functions from source. Plan -> reference existing architecture.
Runtime check: any response using require(), TypeScript syntax, or npm imports is wrong for this project.

---

## Mon, 13 Apr 2026, 10:35 am
**Q:** Project: Setup -- GPRTool

**Coder:** 
── Setup complete for GPRTool ──
  OK   Map:
  OK   Project: Slim
  OK   Project: Brief
  OK   Project: Funcs
  OK   Project: Parse

