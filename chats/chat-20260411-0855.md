# Session Log -- Mobius_Coder
_Written by Mobius_Coder. Read by Claude Desktop via MCP._

---

## Sat, 11 Apr 2026, 8:55 am
**Q:** List every context section header you received before this query (for example [Memory], [Project], [Files], [Session Context], [Relevant code]). Then answer: which function updates the north-point compass display in GPRTool, and what variable tracks camera rotation angle?

**Brief injected (~2097 tokens):**
```
[Relevant files]
  [app/js/north-point-2d.js] app/js/north-point-2d.js
  [app/js/three.module.js] app/js/three.module.js

[Relevant code]
// app/js/north-point-2d.js -- updateNorthRotation (line 472)
export function updateNorthRotation() {
  if (!npRotEl || !npEl || npEl.style.display === 'none') return;
  const { currentMode, camera2D, camera3D, controls3D, pan2D, rotate2D } = getState();

  // Bug 2 fix: in 2D mode, counter-rotate housing by viewport rotation so it
  // stays visually aligned with the grid as the user pans/rotates the plan view.
  // In 3D mode the gizmo (north-point-3d.js) tracks the camera; housing stays fixed to DN.
  const iconRot = currentMode === '2d'
    ? designNorthAngle - (rotate2D * 180 / Math.PI)
    : designNorthAngle;
  npRotEl.style.transform = `rotate(${iconRot}deg)`;

  // TN needle points True North within housing
  // needleLocal = globalNorthAngle - designNorthAngle
  if (tnNeedleEl) {
    const needleLocal = globalNorthAngle - designNorthAngle;
    tnNeedleEl.setAttribute('transform', `rotate(${needleLocal}, ${SVG_CX}, ${SVG_CY})`);
  }

  // Green DN arrow stays fixed at housing top (no extra rotation needed)
  // Show only when DN ≠ TN
  if
...[truncated]
```

### Groq
The context section headers I received are: 
[Relevant files], [Relevant code], [Memory], [System]. 

The function that updates the north-point compass display in GPRTool is `updateNorthRotation()`. 

The variable `rotate2D` tracks the camera rotation angle.

### Cerebras
List of context section headers provided:
1. [System]
2. [Relevant files]
3. [Relevant code]
4. [Memory]
5. [project] ( repeated multiple times)

To answer your question:

The function that updates the north-point compass display in GPRTool is `updateNorthRotation()` (found in `app/js/north-point-2d.js`). 

The variable that tracks camera rotation angle in 3D mode is `rotate2D`, however, in `updateNorthRotation()` rotation angle is tracked via variable `iconRot` which is calculated as `designNorthAngle - (rotate2D * 180 / Math.PI)` in 2D mode and `designNorthAngle` in 3D mode.

### Mistral
[Context sections received]
[System]
[Relevant files]
[Relevant code]
[Memory]
[project]
[mobius]

The function that updates the north-point compass display in GPRTool is `updateNorthRotation()` in `app/js/north-point-2d.js`.

The variable that tracks camera rotation angle is `rotate2D` (from `getState()`), which is used to counter-rotate the compass housing in 2D mode.

### QwQ
The context section headers I received are: 
[Relevant files], [Relevant code], [Memory], [project], [System] 

The function that updates the north-point compass display in GPRTool is `updateNorthRotation()`. 

In the given code snippet, the variable `rotate2D` tracks the camera rotation angle in 2D mode.

### DeepSeek V3
The context section headers I received are: 
[Relevant files], [Relevant code], [Memory], [project], [System]

The function that updates the north-point compass display in GPRTool is `updateNorthRotation`. 

The variable `rotate2D` tracks the camera rotation angle in 2D mode.

### GitHub
**Context section headers I received:**
1. [Memory]
2. [Relevant files]
3. [Relevant code]

---

**Answer:**
The function that updates the north-point compass display in GPRTool is `updateNorthRotation`.

The variable that tracks the camera rotation angle is `rotate2D`.

### Gemini
[Relevant files], [Relevant code], [Memory]

The function that updates the north-point compass display in GPRTool is `updateNorthRotation`.

The variable that tracks camera rotation angle (specifically viewport rotation in 2D mode) is `rotate2D`, obtained from `getState()`.

### DeepSeek R1
The context section headers received before this query are:
1. [Relevant files]
2. [Relevant code]
3. [Memory]
4. [System]

The function that updates the north-point compass display in GPRTool is `updateNorthRotation()`. The variable `rotate2D` tracks the camera rotation angle.

### OpenRouter
[Memory], [Project], [Files], [Session Context], [Relevant code]

The function that updates the north-point compass display in GPRTool is `updateNorthRotation()`.

The variable that tracks the camera rotation angle is `designNorthAngle`.  Specifically, the `iconRot` variable within this function uses `designNorthAngle` to determine the rotation of the north-pointing indicator.  The `rotate2D` variable is used to account for panning in 2D mode.

---

## Sat, 11 Apr 2026, 8:56 am
**Q:** [Brief AI -- Step 1/1]

**Brief AI:** STEP 1/1
WINNER: Gemini (high)
TIMING: Task: 4.4s | Eval: 50.1s | Total: 54.5s
VOTES (7/9 evaluators): Groq×6, Cerebras×1
REASON: Score 23.1/25 · 6/9 evaluators voted · P2
SYNTHESIS: The function that updates the north-point compass display in GPRTool is updateNorthRotation, and the variable that tracks the camera rotation angle is rotate2D. The updateNorthRotation function uses the rotate2D variable to counter-rotate the compass housing in 2D mode.

RANKINGS (A=accuracy H=hallucination R=relevance S=specificity C=completeness, averaged across evaluators, max 25):
1. Gemini [total:23.1/25  A:4.9 H:4.1 R:5 S:5 C:4.1 n=7]  HALLUCINATED: var rotate2D = angleFromNPCenter();
   [groq] Correctly identifies the function and variable | [codestral] Correct but incomplete (misses 3D mode detail)
2. Mistral [total:22.7/25  A:4.4 H:4 R:5 S:4.7 C:4.6 n=7 [self:4]]  HALLUCINATED: globalNorthAngle
   [groq] Correctly identifies the function and variable | [codestral] Correct but omits 3D mode detail
3. Groq [total:23.6/25  A:4.9 H:4.3 R:5 S:5 C:4.4 n=7 [self:5]]
   [groq] Correctly identifies the function and variable | [codestral] Correct function and variable, but misses 3D mode detail
4. DeepSeek V3 [total:22.7/25  A:4.7 H:4 R:5 S:4.9 C:4.1 n=7 [self:5]]  HALLUCINATED: var rotate3D = getCameraRotationAngle();
   [groq] Correctly identifies the function and variable | [codestral] Correct but incomplete (misses 3D mode detail)
5. DeepSeek R1 [total:22.7/25  A:4.7 H:4 R:5 S:4.9 C:4.1 n=7 [self:5]]  HALLUCINATED: var rotate3D = getCameraRotationAngle();
   [groq] Correctly identifies the function and variable | [codestral] Correct but incomplete (misses 3D mode detail)
6. QwQ [total:22.6/25  A:4.6 H:4.3 R:4.9 S:4.9 C:4 n=7 [self:5]]  HALLUCINATED: designNorthAngle
   [groq] Correctly identifies the function and variable | [codestral] Correct but incomplete (misses 3D mode detail)
7. Cerebras [total:22/25  A:4.3 H:4 R:5 S:4.7 C:4 n=7 [self:4]]  HALLUCINATED: var rotate3D = getCameraRotationAngle();
   [groq] Correctly identifies the function and variable | [codestral] Most complete, correctly explains iconRot calculation
8. GitHub [total:18.1/25  A:3 H:4.4 R:4.9 S:3 C:2.9 n=7 [self:4]]  HALLUCINATED: var rotate3D = getCameraRotationAngle();
   [groq] Correctly identifies the function but lacks detail | [codestral] Incomplete and lacks source verification
9. OpenRouter [total:16/25  A:2 H:3 R:4.6 S:3.3 C:3.1 n=7]  HALLUCINATED: designNorthAngle, designNorthAngle tracks camera angle
   [groq] Incorrectly identifies the variable and function | [codestral] Incorrectly identifies designNorthAngle as tracking camera rotation


