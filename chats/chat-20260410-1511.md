# Session Log -- Mobius_Coder
_Written by Mobius_Coder. Read by Claude Desktop via MCP._

---

## Fri, 10 Apr 2026, 3:12 pm
**Q:** In js/commands.js the sendToAI function builds context before sending to the AI model. Show the exact code for how it calls getCodeContext and getMemoryContext, what it does with the results, and how it assembles the final prompt. Does it pass the brief to autoEvaluate?

**Brief injected (~1586 tokens):**
```
[Relevant files]
  [js/commands.js] js/commands.js
  [api/auth/[service].js] api/auth/[service].js

[Relevant code]
// js/commands.js -- sendToAI (line 183)
async function sendToAI(model, messages, output, outputEl, options = {}) {
  const userId = getAuth('mobius_user_id');
  const start  = Date.now();
  output('Thinking...');

  // Build context-prepended query: memory + brief + slim + last-read file + user query.
  // CLAUDE.md excluded -- contains MCP/Desktop instructions not relevant to cloud AI.
  let _finalQuery = messages[messages.length - 1].content;
  const _ctx   = window._projectContext;
  const _parts = [];
  if (window.getMemoryContext) {
    try {
      const _mem = await window.getMemoryContext(_finalQuery);
      if (_mem) _parts.push('[Memory]\n' + _mem);
    } catch { /* never block on memory */ }
  }
  if (window.getCodeContext) {
    try {
      const _code = await window.getCodeContext(_finalQuery);
      if (_code) _parts.push(_code);
    } catch { /* never block on code index */ }
  }
  if (_ctx) {
    if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
    if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
  }
  if (window.lastReadFile && window.l
...[truncated]
```

### Cerebras Llama 3.1 8B
The exact code for how it calls `getCodeContext` and `getMemoryContext`, assembles the final prompt, and passes the brief is as follows:

```javascript
// js/commands.js -- sendToAI (lines 185-198)
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}
```

Here's what it does:

1. It attempts to call `getMemoryContext` with the current `_finalQuery` as an argument. If it is successful, it prepends the result to the `_parts` array.
2. It attempts to call `getCodeContext` with the current `_finalQuery` as an argument. If it is successful, it appends the result to the `_parts` array.
3. If a project context `_ctx` is available, it checks if the context has a brief. If it does, it appends the brief to the `_parts` array. It also checks if the context has a slim section, and if it does, it appends that to the `_parts` array.

Finally, it assembles the final prompt by joining the contents of the `_parts` array with newline characters (`\n\n`) and prepending the result to the current `_finalQuery`:

```javascript
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

As for the `autoExtractMemory` function, it is passed as an argument to the `sendToAI` function within the context of the `window` object, which suggests that it is a window-scoped function. However, there is no code that shows the function being called with the brief. 

Instead, the brief is only appended to the final prompt after it has been extracted from the context with `getCodeContext`:

```javascript
// No call to autoExtractBrief with the brief
if (window.autoExtractMemory) {
  window.autoExtractMemory(messages[messages.length - 1].content, reply);
}
```

This suggests that the `autoExtractMemory` function is used to extract memory-related information from the AI response, rather than the brief.

### Groq Llama 3.1 8B
Here's the exact code from `js/commands.js` where `sendToAI` function calls `getCodeContext` and `getMemoryContext`:

```javascript
// js/commands.js -- sendToAI (lines 187-206)
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}
```

And then it assembles the final prompt by prepending the context to the query:

```javascript
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

Finally, it passes the final prompt to the AI model:

```javascript
const res = await fetch('/ask', {
  method:  'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    query:   _finalQuery,
    model,
    userId,
    history: [...conversationHistory, ...messages.slice(0, -1)]
  })
});
```

It does not pass the brief to `autoEvaluate()`, but it does pass the brief to the AI model when it finds a corresponding project context (`_ctx`):

```javascript
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
```

However, when building the final prompt, it does not append additional context to the query, it only prepends the context. Therefore, the brief will only be included as context if it's specified in the project context, but it will not be passed to `autoEvaluate()` explicitly.

### Codestral
Here's the exact code for how `sendToAI` calls `getCodeContext` and `getMemoryContext`, processes the results, and assembles the final prompt:

```javascript
// Memory context
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

// Code context
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}

// Project context
if (_ctx) {
  if (_ctx.brief) _parts.push('[Project]\n' + _ctx.brief);
  if (_ctx.slim)  _parts.push('[Files]\n'   + _ctx.slim);
}

// Last read file
if (window.lastReadFile && window.lastReadFile.content) {
  const c = window.lastReadFile.content;
  _parts.push('[File: ' + window.lastReadFile.path + ']\n'
    + (c.length > 8000 ? c.slice(0, 8000) + '\n...[truncated]' : c));
}

// Assemble final query
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

Key points:
1. Both `getMemoryContext` and `getCodeContext` are called with the final query as input
2. Results are added to `_parts` array with appropriate section headers
3. The brief and slim project context are added next
4. The last read file content is added (truncated if >8000 chars)
5. All parts are joined with double newlines and prepended to the original query
6. The final assembled prompt is sent to the AI model

No, it does not pass the brief to `autoEvaluate`. The brief is included in the context but not directly passed to that function. The brief is used to provide project context to the AI model, while `autoEvaluate` handles memory extraction separately.

### Mistral Codestral (fallback from Groq Llama 3.3 70B) (fallback from Gemini)
Here's the exact code for how `sendToAI` calls `getCodeContext` and `getMemoryContext`, processes their results, and assembles the final prompt:

```javascript
// Memory context call
if (window.getMemoryContext) {
  try {
    const _mem = await window.getMemoryContext(_finalQuery);
    if (_mem) _parts.push('[Memory]\n' + _mem);
  } catch { /* never block on memory */ }
}

// Code context call
if (window.getCodeContext) {
  try {
    const _code = await window.getCodeContext(_finalQuery);
    if (_code) _parts.push(_code);
  } catch { /* never block on code index */ }
}
```

The results are:
1. Memory context is prepended with `[Memory]` header
2. Code context is added as-is (no header)
3. Both are pushed into the `_parts` array

The final prompt assembly happens here:
```javascript
if (_parts.length) _finalQuery = _parts.join('\n\n') + '\n\n' + _finalQuery;
```

The code does **not** pass the brief to `autoEvaluate`. The brief is only included in the context if it exists in `window._projectContext.brief` and is added to `_parts` with a `[Project]` header.

### OpenRouter
[Error: HTTP 500]

