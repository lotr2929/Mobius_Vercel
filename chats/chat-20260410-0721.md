# Session Log -- Mobius_Coder
_Written by Mobius_Coder. Read by Claude Desktop via MCP._

---

## Fri, 10 Apr 2026, 7:24 am
**Q:** Plan the NPoint compass fix for GPRTool. Two bugs confirmed in app/js/north-point-2d.js. Bug 1: the np-ctx-rotate-np click handler calls enterRotateMode then showDNInput which calls applyDesignNorth -- this sets designNorthAngle (DN) but per spec it should set globalNorthAngle (TN). The drag rotation in handleDrag also calls applyDesignNorth when isRotating. Both paths need to call applyGlobalNorth instead. Need a separate Set Design North menu item for applyDesignNorth. Bug 2: updateNorthRotation destructures currentMode camera2D camera3D controls3D pan2D from getState but not rotate2D -- iconRot is set to just designNorthAngle but in 2D mode should be designNorthAngle minus (rotate2D * 180 / Math.PI). What is the safest fix order and what else does each change touch?

**Brief injected (~2456 tokens):**
```
[Relevant files]
  [app/js/north-point-2d.js] app/js/north-point-2d.js
  [_dev/Three.js Grid Orientation.md] _dev/Three.js Grid Orientation.md
  [app/js/three.module.js] app/js/three.module.js

[Relevant code]
// _context/chat.md -- Debugging Steps and Code Changes (line 2506)
### Debugging Steps and Code Changes

The primary issue is the `handleDrag` function incorrectly modifying `designNorthAngle` during the `rotateMode` interaction. We need to redirect this interaction to `globalNorthAngle`. Additionally, the user interface for "Rotate N Point" is currently confusing, as it opens an input for "Design North".

#### 1. Add a New Module State Variable

We need a variable to store the `globalNorthAngle` at the start of the rotation, similar to how `rotateStartDN` (which currently holds `designNorthAngle`) is used.

**File: app/js/north-point-2d.js**

```javascript
// ── Module state ─────────────────────────────────────────────
let npEl, npRotEl, npCtxEl, npVP;
let npW = NP_BASE_W;
let getState;

let designNorthAngle = 0;  // grid orientation — changed by "Set Design North"
let globalNorthAngle = 0;  // TN offset from world -Z — changed by "Rotate N Point"
let dnGroupEl  = null;  /
...[truncated]
```

### Cerebras Llama 3.1 8B
**Plan for Fixing NPoint Compass Bugs in `app/js/north-point-2d.js`**

**Bug 1: Incorrect globalNorthAngle Update**

1. Create a new, separate "Set Design North" menu item that calls `applyDesignNorth` to set `designNorthAngle`.
2. Update the `handleDrag` function in `rotateMode` to call `applyGlobalNorth` to set `globalNorthAngle`, instead of `applyDesignNorth`.
3. Update the `onDragDown` function to set `rotateStartGlobalNorth` to the current value of `globalNorthAngle` when the rotation drag starts.

**Bug 2: 2D Mode Rotation Calculation**

1. Update the `onModeChange` function to handle the change to 2D mode by setting `rotate2D` to the angle of the icon in 2D mode.
2. Update the `updateNorthRotation` function to calculate the rotation of the icon in 2D mode by subtracting `rotate2D * 180 / Math.PI` from `designNorthAngle` when in 2D mode.
3. Update the `updateNorthRotation` function to pass `rotate2D` as a variable when destructuring `currentMode` from `getState`.

**Safest Fix Order**

1. Bug 2: Update `updateNorthRotation` to correctly calculate the rotation of the icon in 2D mode.
2. Bug 1: Update the `handleDrag` function to call `applyGlobalNorth` and create a separate "Set Design North" menu item.

**Dependencies**

* Updating `updateNorthRotation` will require retesting the compass rotation in both 3D and 2D modes.
* Updating the menu item and `handleDrag` function will require retesting the rotation drag functionality.

**Alternative Fix Order**

* If you prefer to fix Bug 1 first, you can update the `handleDrag` function and the menu item, then test the rotation drag functionality.
* If you prefer to fix Bug 2 first, you can update `updateNorthRotation` and test the compass rotation in both 3D and 2D modes before updating the menu item and `handleDrag` function.

### OpenRouter (auto-free)
To address the issues in **app/js/north-point-2d.js**, follow this structured approach:

### 1. **Fix the Angular Handling Logic**
- **Problem:** The original code uses `designNorthAngle` for `updateNorthRotation`, but this is incorrect during rotation. It should use `globalNorthAngle`.
- **Fix:** Modify `onDragDown` to invoke `applyGlobalNorth(angle)` and `updateNorthRotation` with the correct angle.
- **Impact:** Ensures grid orientation remains fixed while tolerating TN rotation.

### 2. **Revise `updateNorthRotation` Function**
- **Problem:** The original logic subtracts `designNorthAngle` from `globalNorthAngle`, which is not needed once rotation is handled.
- **Fix:** Use `globalNorthAngle` in `updateNorthRotation` to reflect the true angle for design/grid consistency.
- **Impact:** Removes unnecessary `designNorthAngle` interference.

### 3. **Adjust `applyDesignNorth` and `applyGlobalNorth` Calls**
- **Problem:** `applyDesignNorth` uses `designNorthAngle`, while `applyGlobalNorth` should use `globalNorthAngle`.
- **Fix:** Update both functions to accept `globalNPointAngle` as a parameter, ensuring consistent rotation.
- **Impact:** Aligns design and global North angles during interactions.

### 4. **Ensure UI Properness**
- **Problem:** The DN arrow and needle logic depends on accurate angle values.
- **Fix:** Verify the DN label and arrow display logic updates correctly using `globalNorthAngle`.
- **Impact:** Prevents UI elements from misaligning with the grid.

### Summary of Changes (Key Modifications)
- **Remove `designNorthAngle` from rotation logic** and adopt `globalNorthAngle`.
- **Update `updateNorthRotation`** to call `applyGlobalNorth` and avoid unintended angle settings.
- **Refactor `handleDrag`** to call `applyGlobalNorth` instead of `applyDesignNorth`.

These steps resolve the display errors and ensure the compass behavior matches the intended design.

### Groq Llama 3.3 70B
To fix the two bugs in `app/js/north-point-2d.js`, I propose the following plan:

### Bug 1: Incorrect Angle Modification

1.  **Create a new menu item for "Set Design North"**: This will call `applyDesignNorth` and modify `designNorthAngle` without affecting the "Rotate N Point" functionality.
2.  **Update the click handler for "np-ctx-rotate-np"**: After entering rotate mode, call `showTNInput` (to be implemented) instead of `showDNInput`. This will prompt the user to input the new true north angle.
3.  **Modify the drag rotation in `handleDrag`**: When rotating, call `applyGlobalNorth` instead of `applyDesignNorth` to update `globalNorthAngle`.
4.  **Update `applyGlobalNorth`**: Ensure it correctly sets `globalNorthAngle` and triggers any necessary updates to the north point compass.

### Bug 2: Incorrect Icon Rotation in 2D Mode

1.  **Add `rotate2D` to the destructured variables in `updateNorthRotation`**: Access `rotate2D` from `getState()` and store it in a variable.
2.  **Update the icon rotation calculation**: Modify `iconRot` to `designNorthAngle - (rotate2D * 180 / Math.PI)` when in 2D mode.

### Safest Fix Order

To minimize potential conflicts and ensure a stable debugging process, I recommend fixing Bug 1 first, followed by Bug 2. This order makes sense because:

*   Bug 1 involves modifying the functionality of the "Rotate N Point" interaction, which affects the north point compass directly.
*   Bug 2 is related to the visualization of the compass in 2D mode, which is a separate concern.

By addressing Bug 1 first, we can ensure that the "Rotate N Point" interaction correctly modifies `globalNorthAngle` without affecting the design north angle. Then, by fixing Bug 2, we can accurately display the compass icon rotation in 2D mode.

### Affected Code and Potential Consequences

*   Bug 1 fixes will affect:
    *   `np-ctx-rotate-np` click handler
    *   `handleDrag` function
    *   `applyGlobalNorth` function
    *   Potential impact on `designNorthAngle` and `globalNorthAngle` variables
*   Bug 2 fixes will affect:
    *   `updateNorthRotation` function
    *   Icon rotation calculation
    *   Potential impact on compass visualization in 2D mode

Please confirm this plan before I proceed with implementing the fixes.

### Codestral
# Plan for NPoint Compass Fix

## Bug 1: Incorrect Angle Application
**Problem**: `np-ctx-rotate-np` click handler and drag rotation incorrectly modify `designNorthAngle` when they should modify `globalNorthAngle`.

**Solution**:
1. Create a new menu item "Set Design North" that calls `applyDesignNorth`
2. Modify `np-ctx-rotate-np` to call `applyGlobalNorth` instead of `applyDesignNorth`
3. Update `handleDrag` to call `applyGlobalNorth` during rotation

**Impact**:
- Requires UI changes to add new menu item
- Affects both click and drag interactions
- Maintains existing `designNorthAngle` functionality

## Bug 2: 2D Rotation Calculation
**Problem**: `updateNorthRotation` doesn't account for `rotate2D` in 2D mode.

**Solution**:
1. Add `rotate2D` to destructured state in `updateNorthRotation`
2. Modify icon rotation calculation to include `rotate2D` adjustment

**Impact**:
- Only affects 2D view calculations
- Maintains existing 3D behavior

## Implementation Order
1. First fix Bug 1 (angle application) as it's more fundamental
2. Then fix Bug 2 (2D rotation) as it's a secondary calculation

## Additional Considerations
- Need to ensure all angle calculations remain consistent with Z-up coordinate system
- Verify that `globalNorthAngle` updates properly in both 2D and 3D views
- Test edge cases where angles wrap around 360°

Would you like me to proceed with implementing these changes?

### Gemini 2.5 flash
Here's a plan to fix the two identified bugs in `app/js/north-point-2d.js`, prioritising the most impactful changes first.

### Fix Order and Reasoning

The safest approach is to first correct the underlying data manipulation (Bug 1) to ensure `globalNorthAngle` is modified as intended. Once the data integrity is restored, we can then address the visual display of the compass housing (Bug 2) based on the now-correct angles.

#### Bug 1: NPoint Rotation Incorrectly Modifies `designNorthAngle`

**Description:**
The `np-ctx-rotate-np` click handler, intended for rotating True North, currently calls `enterRotateMode` followed by `showDNInput`, which ultimately sets `designNorthAngle`. The subsequent drag interaction in `handleDrag` also modifies `designNorthAngle` when `isRotating`. Both of these should instead modify `globalNorthAngle`. The UI for "Rotate N Point" should trigger a drag interaction for `globalNorthAngle` and not open an input for "Design North".

**Impact:** Incorrect True North representation, confusing user interface.

**Proposed Changes:**

1.  **Modify Module State:**
    *   **File:** `app/js/north-point-2d.js`
    *   **Change:** Introduce `rotateStartGlobalNorth` to store `globalNorthAngle` at the start of a rotation.
    *   **Code:**
        ```javascript
        // ── Module state ─────────────────────────────────────────────
        let npEl, npRotEl, npCtxEl, npVP;
        let npW = NP_BASE_W;
        let getState;

        let designNorthAngle = 0;  // grid orientation — changed by "Set Design North"
        let globalNorthAngle = 0;  // TN offset from world -Z — changed by "Rotate N Point"
        let dnGroupEl  = null;  // green DN arrow (injected)
        let dnLabelEl  = null;  // angle label
        let tnNeedleEl = null;  // TN needle group (from SVG)

        let isDragging      = false;
        let dragPending     = false;
        let dragStartClient = { x: 0, y: 0 };
        let dragOffset      = { x: 0, y: 0 };

        let rotateMode      = false;
        let isRotating      = false;
        let rotateStartAngle = 0;
        let rotateStartGlobalNorth = 0; // NEW: Store globalNorthAngle at the start of rotation
        // let rotateStartDN    = 0; // REMOVE/DEPRECATE: This variable will no longer be used for rotation calculation.

        let isResizing   = false;
        let resizeHandle = null;
        let resizeStart  = { w: NP_BASE_W, anchor: { x: 0, y: 0 } };

        const _npO = new THREE.Vector3();
        const _npN = new THREE.Vector3();
        ```
    *   **Reasoning:** Provides a dedicated variable to correctly capture the initial `globalNorthAngle` for rotation calculations, aligning with the new interaction logic.

2.  **Update `onDragDown` for `rotateMode`:**
    *   **File:** `app/js/north-point-2d.js`
    *   **Change:** When `rotateMode` is active, store the current `globalNorthAngle` in `rotateStartGlobalNorth`.
    *   **Code (within `onDragDown`):**
        ```javascript
        if (rotateMode) {
            rotateStartGlobalNorth = globalNorthAngle; // Capture current globalNorthAngle
            // OLD: rotateStartDN = designNorthAngle; // Remove or comment out this line
        }
        ```
    *   **Reasoning:** Ensures the base angle for `globalNorthAngle` rotation is correctly initialised before dragging.

3.  **Update `handleDrag` for `rotateMode`:**
    *   **File:** `app/js/north-point-2d.js`
    *   **Change:** When `rotateMode` and `isRotating` are true, calculate the new angle based on `rotateStartGlobalNorth` and apply it using `applyGlobalNorth`.
    *   **Code (within `handleDrag`):**
        ```javascript
        if (rotateMode && isRotating) {
            // Calculate dragAngle (this part remains the same)
            const dx = dragOffset.x;
            const dy = dragOffset.y;
            const center = { x: npW / 2, y: npW / 2 }; // Center of the compass housing
            const startVec = new THREE.Vector2(dragStartClient.x - npVP.left - center.x, npVP.bottom + npVP.height - dragStartClient.y - center.y);
            const currentVec = new THREE.Vector2(dragStartClient.x + dx - npVP.left - center.x, npVP.bottom + npVP.height - (dragStartClient.y + dy) - center.y);
            const dragAngle = Math.atan2(currentVec.y, currentVec.x) - Math.atan2(startVec.y, startVec.x); // In radians
            const dragAngleDeg = dragAngle * 180 / Math.PI;

            let newAngle = rotateStartGlobalNorth + dragAngleDeg; // Use rotateStartGlobalNorth
            applyGlobalNorth(newAngle); // Apply to globalNorthAngle
            // OLD: applyDesignNorth(newAngle); // Remove or comment out this line
        }
        ```
    *   **Reasoning:** Redirects the drag-based rotation to the correct angle variable (`globalNorthAngle`) and uses the appropriate setter function (`applyGlobalNorth`).

4.  **Refactor "Rotate N Point" UI Interaction:**
    *   **File:** `app/js/north-point-2d.js` (or related UI event listeners)
    *   **Change:** Locate the click handler for `np-ctx-rotate-np`. It should only call `enterRotateMode()` to enable the drag functionality. Remove any calls to `showDNInput()` from this handler.
    *   **Code (conceptual, as the exact handler isn't provided, but assuming an event listener for `.np-ctx-rotate-np`):**
        ```javascript
        // Assuming this is inside an initialisation function
        document.querySelector('.np-ctx-rotate-np').addEventListener('click', () => {
            enterRotateMode();
            // OLD: showDNInput(); // REMOVE this line
            // If a numerical input for globalNorthAngle is ever needed, it would be a separate UI element/function.
        });
        ```
    *   **Reasoning:** Resolves the confusing UI where "Rotate N Point" opened an input for "Design North". The interaction becomes purely drag-based for `globalNorthAngle`. A dedicated "Set Design North" menu item would be needed for `applyDesignNorth` via numerical input, as noted in the problem statement, but this is a separate UI addition, not part of fixing the existing bug.

---

#### Bug 2: Incorrect Compass Housing Rotation in 2D Mode

**Description:**
In `updateNorthRotation`, `rotate2D` is not destructured from `getState()`. The compass housing's rotation (`npRotEl.style.transform`) is currently incorrect in 2D mode, being set to `designNorthAngle` only. It needs to account for the 2D camera's rotation (`rotate2D`).

**Impact:** The compass housing does not accurately reflect the `designNorthAngle` relative to the screen's vertical when the 2D camera is rotated.

**Proposed Changes:**

1.  **Add `rotate2D` to `getState()` destructuring:**
    *   **File:** `app/js/north-point-2d.js`
    *   **Change:** Include `rotate2D` in the destructured variables within `updateNorthRotation`.
    *   **Code (within `updateNorthRotation`):**
        ```javascript
        export function updateNorthRotation() {
          if (!npRotEl || !npEl || npEl.style.display === 'none') return;
          // Add rotate2D to destructuring
          const { currentMode, camera2D, camera3D, controls3D, pan2D, rotate2D } = getState(); // MODIFIED LINE
          // ... rest of the function ...
        }
        ```
    *   **Reasoning:** Makes the 2D camera's rotation available for compass housing calculations.

2.  **Adjust `npRotEl` Transform in `updateNorthRotation`:**
    *   **File:** `app/js/north-point-2d.js`
    *   **Change:** Modify the `npRotEl.style.transform` to account for `rotate2D` when in 2D mode.
    *   **Code (within `updateNorthRotation`):**
        ```javascript
        export function updateNorthRotation() {
            if (!npRotEl || !npEl || npEl.style.display === 'none') return;
            const { currentMode, camera2D, camera3D, controls3D, pan2D, rotate2D } = getState();

            // Set compass housing rotation
            let iconRot = designNorthAngle;
            if (currentMode === '2D') {
                // In 2D mode, compensate for the camera's rotation so designNorthAngle aligns with screen vertical
                iconRot = designNorthAngle - (rotate2D * 180 / Math.PI);
            }
            npRotEl.style.transform = `rotate(${iconRot}deg)`; // MODIFIED LINE
            // OLD (from chat.md snippet, commented out): // Remove housing rotation — keep fixed to grid orientation // npRotEl.style.transform = `rotate(${designNorthAngle}deg)`;

            // TN needle points True North within housing
            if (tnNeedleEl) {
                const needleLocal = globalNorthAngle - designNorthAngle;
                tnNeedleEl.setAttribute('transform', `rotate(${needleLocal}, ${SVG_CX}, ${SVG_CY})`);
            }

            // Green DN arrow stays fixed at housing top (no extra rotation needed relative to housing)
            if (dnGroupEl && dnLabelEl) {
                dnGroupEl.setAttribute('transform', `rotate(0, ${SVG_CX}, ${SVG_CY})`);
                dnGroupEl.style.display = designNorthAngle !== globalNorthAngle ? '' : 'none';
                // ... rest of DN label positioning code ...
            }
        }
        ```
    *   **Reasoning:** This ensures that the compass housing correctly displays `designNorthAngle` relative to the screen's vertical, compensating for any camera rotation in 2D mode. The `designNorthAngle` on the compass will then always point in the visual direction of design north on the screen.

---

